# AstroTool v5.0.0 prototype — Wave 1 release evidence

State: **NOT RELEASED.** This document collects the Wave 1 gate evidence. The
prototype may be called ready for own-device trial only when every section
below is filled with a pass, including the external gates.

## Scope

Offline iPhone companion exchanging an encrypted `.astromobile` AirDrop
document with the Mac. The Mac remains the sole owner of the photo library;
only checklist completions and notes return from the phone; no CloudKit,
TestFlight, or background execution; Personal Team install with the seven-day
provisioning limit.

## Machine gate (this repository, macOS)

Last verified evidence per check; a check counts only when re-run green on the
final release tree in one sitting.

| Check | Result | Evidence |
|---|---|---|
| `swift test --no-parallel` | ✅ 2026-08-24 at `b754f86` | 3524 tests, 221 suites, 0 failures; re-run on the final tree pending (deferred at owner request — heavy runs paused) |
| `xcodegen generate` diff | ✅ 2026-08-24 | regenerated project byte-identical (empty `git status`) |
| `xcodebuild test -scheme AstroTool -destination platform=macOS` | pending | deferred: launching the Mac app's test host takes over the machine; run when the owner allows |
| `xcodebuild test -scheme AstroToolMobile -destination iOS Simulator` | pending | runnable locally: iOS 26.4.1 simulator runtime (23E254a) is installed and the deployment target is iOS 26.0; deferred until the owner allows heavy runs |
| `./scripts/check-public-content.sh` | ✅ 2026-08-24 at `7d50220` | exit 0 |
| `./scripts/smoke-mobile-package.sh` | ✅ 2026-08-24 at `cb34711` | SMOKE OK; source manifest bit-identical; forbidden-byte scan clean (2 tests) |
| `git diff --check` | ✅ 2026-08-24 | clean |

## External gates (unfilled — do not claim)

- **iOS 26.5 build/runtime**: compile and run `AstroToolMobileTests` and the
  UI tests through the generated Xcode graph on a machine with the iOS 26.5
  platform. Result: _pending_.
- **Own-device acceptance** (fill during the physical test): install with
  Personal Team; AirDrop a real metadata-only package with previews disabled;
  enable Airplane Mode; open the briefing; toggle one checklist item; add one
  note; AirDrop the return package; approve it on the Mac; verify the source
  library manifest is unchanged. Record only anonymous counts and pass/fail:
  - packages exchanged: _pending_
  - checklist changes applied: _pending_
  - notes applied: _pending_
  - source library manifest identical before/after: _pending_
  - result: _pending_
