# Astro-Tool — végrehajtási állapot

> **Folytatáshoz (új Claude session):** olvasd el a
> `docs/superpowers/plans/2026-08-01-astro-tool.md` tervet, ezt a fájlt, és a
> `docs/superpowers/specs/2026-08-01-astro-tool-design.md` specet. Végrehajtás:
> superpowers:subagent-driven-development — taskonként friss Sonnet implementer
> subagent (teljes task-szöveg a promptban), utána review; commit+push minden
> task után; utána EZT a fájlt frissíteni és pusholni. A main-en dolgozunk
> (user jóváhagyta). A /Volumes/images TCC-blokkolt — mindent fixture-fákon
> tesztelünk, a valós könyvtárhoz hozzá sem érünk. VASSZABÁLY: a képkönyvtárban
> semmit nem törlünk/mozgatunk soha.

| Task | Állapot | Megjegyzés |
|---|---|---|
| T1 SPM skeleton + Model | ✅ kész (1f427fe) | 3 teszt zöld |
| T2 Sanitizer + SessionDateParser | ✅ kész | 26 teszt zöld (3 T1 + 23 új) |
| T3 Config | ✅ kész | 37 teszt zöld (26 T1+T2 + 11 új) |
| T4 WriteGuard | ✅ kész | 46 teszt zöld (37 T1+T2+T3 + 9 új) |
| T5 SQLite réteg | ✅ kész | 69 teszt zöld (46 T1-T4 + 23 új) |
| T6 PathClassifier + Scanner | ✅ kész | 89 teszt zöld (69 T1-T5 + 20 új) |
| T7 FITS parser | ✅ kész | 102 teszt zöld (89 T1-T6 + 13 új) |
| T8 ImageIO meta | ✅ kész | 109 teszt zöld (102 T1-T7 + 7 új) |
| T9 Audit motor | ✅ kész | 124 teszt zöld (109 T1-T8 + 15 új) |
| T10 Duplikátum | ✅ kész | 138 teszt zöld (132 T1-T9+R1 + 6 új) |
| T11 Suggestion script | ✅ kész | 148 teszt zöld (138 T1-T10+R1 + 10 új) |
| T12 Stats + wide-field | ✅ kész | 163 teszt zöld (148 T1-T11 + 15 új) |
| T13 Calib | ✅ kész | 172 teszt zöld (163 T1-T12 + 9 új) |
| T14 Session-párosítás | ✅ kész | 179 teszt zöld (172 T1-T13 + 7 új) |
| T15 Rate | ✅ kész | 199 teszt zöld (179 T1-T14 + 20 új) |
| T16 CLI | ✅ kész | 214 teszt zöld (203 T1-T15 + 11 új) |
| T17 SwiftUI app | ✅ kész | 214 teszt zöld (UI-hoz nincs unit teszt); build+release build zöld, app elindul |
| T18 build.sh + DMG | ✅ kész | 214 teszt zöld; build.sh: swift build -c release → AstroTool.app + astrotool CLI, ad-hoc codesign, DMG + CLI zip, ~/Applications install, ~/.local/bin/astrotool symlink; ikon icon/make_icon.swift-ből (CoreGraphics, determinisztikus csillagmező + mappa-motívum) |
| T19 CI + README | ✅ kész | 214 teszt zöld; release.yml (macos-15, swift test → build.sh → gh release create), LICENSE (MIT), CHANGELOG.md (Keep-a-Changelog, 0.1.0), README.md; build.sh: `${CI:-}` guard az `~/Applications` install és `~/.local/bin` szimlink lépésekre |
| T20 Pages + v0.1.0 | ✅ kész (részben) | Release: https://github.com/themokx1/Astro-Tool/releases/tag/v0.1.0 (AstroTool.dmg + astrotool.zip, CI zöld); docs/ oldal kész és pusholva, de a GitHub Pages **nincs bekapcsolva** — a repo private, a jelenlegi GitHub plan nem támogatja Pages-t private repóhoz (API 422 "Your current plan does not support GitHub Pages for this repository"). Teendő: repo publikussá tétele VAGY plan-váltás, utána `gh api -X POST repos/themokx1/Astro-Tool/pages -f 'source[branch]=main' -f 'source[path]=/docs'`. |
| Review R1 (T6–T9) javítások | ✅ kész | 132 teszt zöld (124 + 8 új) |
| Review R2 (T10–T15) javítások | ✅ | 203 teszt zöld (199 + 4 új) |
| Final review javítás (TCC exit 2) | ✅ | 216 teszt zöld (214 + 2 új); WriteGuard (createSessionTree/writeToolFile) és astrotool Commands.makeDatabase most `AstroError.accessDenied`-re fordítja a permission-hibákat (EPERM/EACCES, `isPermissionError` publikussá téve), így a chmod 555 gyökér is exit 2 + magyar TCC útmutatót ad exit 1 helyett; AppState.endOperation stale-completion guard (UUID operation-id) |
| Valós-könyvtár javítások R1 | ✅ | 231 teszt zöld (216 + 15 új); `PathClassifier` sekély-útvonal mélységi javítás (sessions/stacks/processed) + egyes számú session role-alkönyvtárak (light/flat/dark/bias); `Scanner` lazán heverő dátum-mappa-frame szerep-finomítás FITS IMAGETYP-ből + EPERM-ellenálló bejárás (`ScanSummary.inaccessiblePaths`, `Database.markMissing(excludingPrefixes:)`); új `loose-frames-in-date-dir` audit szabály; `StatsQueries` regresszió (.DS_Store nem lesz célpont); `AppState.runScan()` a sikeres scan után frissíti a Stats/Calib fület is |
| Ground-truth verifikáció + javítások | ✅ kész | 238 teszt zöld (231 + 7 új); a valós `add_new_session.sh` + `tools/rate/LightFrameRater.py` alapján: `Sanitizer` törlés-szemantikára javítva (`tr -cd`, nem `tr -c ... _`); `WriteGuard.createSessionTree` mostantól a teljes fát építi (`stacks/<T>/<D>`, `processed/<T>/<D>`, `calibration_library/{darks,flats,biases}` mkdir -p); új `Sources/AstroCore/NewSession/SessionCreator.swift` (sanitize+dátum-validáció+valódi README-sablon+fa-létrehozás egy helyen, CLI+app rá van kötve, duplikált logika törölve); új `AstroConfig.toolOutputDirNames` + `ToolOutputRule` audit szabály (Stack/Review/Reject/light_frame_rating_report_assets felismerése `tool-output`/`probablyIntentional`-ként, a noncanonical-subdir/assets-without-date/loose-frames-in-date-dir szabályok kihagyják őket); `Rater` pontozás mostantól exptime-csoportonként (0.1s kerekítve) z-score-ol, nem a teljes batch-en át — a `tools/rate` proven viselkedését követve. |
| Valós-könyvtár javítások R2 | ✅ kész | 245 teszt zöld (238 + 7 új); `Scanner`: az `unchanged` gyors-út mostantól újraszámolja a `PathClassifier` kimenetét (area/target/sessionDate/role) + a `kind`-öt is a már meglévő fájlokra, és a DB-sort helyben gyógyítja, ha eltér a tároltól — így egy classifier-javítás után a korábban beolvasott sorok nem maradnak örökre elavult besorolással (a valós DB-ben talált `target=".DS_Store"` jellegű sorok esete); a lazán heverő keret (loose-frame) IMAGETYP-alapú szerep-finomítása védett: ha a tárolt szerep konkrét keret-szerep (light/flat/dark/bias) és a tiszta útvonal-osztályozó `.other`-t adna, a tárolt szerep megmarad (nem degradálódik vissza); új `ScanSummary.reclassified: Int` mező (additív, alapértelmezett 0), a CLI `scan` human kimenete `", reclassified N"`-t ír ki, ha N > 0. `ImageMetaReader`/`ImageMeta`: új `exposureSeconds`/`iso` mező (Exif `ExposureTime`/`ISOSpeedRatings`); a `Scanner.captureMeta` CR3/TIF ágon ezeket a `fits_meta.exptime`/`gain` oszlopokba menti (a DSLR-keretek FITS `EXPTIME` nélkül eddig `exposureBreakdown["unknown"]`-ban landoltak, 0 integrációs idővel) — a `StatsQueries` változtatás nélkül veszi figyelembe őket, mivel csak a `fits_meta.exptime`-ot nézi. |

| `--refresh-meta` backfill | ✅ | 249 teszt zöld (245 + 4 új); `LibraryScanner.scan(refreshMeta:)` + `ScanSummary.metaRefreshed`: változatlan meta-hordozó fájlokra (fit/fits/fz/cr3/tif) pótolja a hiányzó `fits_meta`-t (nincs sor VAGY raw/image üres exptime-mal) — a Canon EXIF-expozíció backfillje a régebben scannelt CR3-akra; CLI `scan --refresh-meta` kapcsoló |
| Audit memória-javítás | ✅ | 251 teszt zöld (249 + 2 új); gyökérok: `DuplicateFinder.sha256Hash` chunkolt olvasása nem volt `autoreleasepool`-ba csomagolva (Darwin Data/NSData-hidalás miatt a chunk-pufferek a futás végéig életben maradtak) + a méret-előszűrő azonos kamerás FITS köteteken gyakorlatilag mindent átengedett teljes hashelésre. Mért csúcs-RSS (60×5 MiB szintetikus fixture, `/usr/bin/time -l`): **javítás előtt +301,5 MiB delta** (343 MB RSS csúcs), **javítás után +2,3 MiB delta** (186 MB RSS csúcs). Javítás: `autoreleasepool` minden chunk olvasás körül (`DuplicateFinder`, `FITSReader.readOneHeader`, `Rater.rate`/`NativeStats.compute` hívás), + új prefix-hash réteg (első 64 KiB, streamelve) a `DuplicateFinder`-ben — csak (méret, prefix) ütköző csoportok kapnak teljes SHA-256-ot, a `content_hash` cache-viselkedése változatlan. |
| R3 Session-részletek (gyújtótáv, kamera, gain, filter) | ✅ | 258 teszt zöld (251 + 7 új); új `Sources/AstroCore/Stats/SessionStats.swift` (`SessionDetail`, `SessionStatsQueries.sessions(target:db:config:)`) — session-önkénti (`sessions/<target>/<date>/`) bontás: keret-számok szerep szerint, integráció + expozíció-bontás csak a light keretekből (ugyanaz a konvenció, mint `TargetStats`), plusz distinct gyújtótávolság (1 mm-re kerekítve), kamera, gain/ISO, szenzor hőm. (0.5°C-ra kerekítve), szűrő és `hasReadme`. CLI: `stats --target T --sessions [--json]` (`--sessions` `--target` nélkül exit 1). App: `AppState.sessionDetails`/`selectedTarget`/`loadSessionDetails(target:)`, `StatsView` táblasor-kiválasztás alatti `SessionDetailPanel` (magyar címkékkel). |
| R3 Tagelés (DB séma v2, CLI `tag`, app tag-chipek) | ✅ | 281 teszt zöld (258 + 23 új); `Database.migrate()` verzió-lépcsős lett (`if version < 1 { … }` majd `if version < 2 { … }`) — friss DB egyből v2-re fut, egy éles v1 DB (séma+adat) a helyben lévő sorok érintetlenül hagyása mellett kap `tags` táblát és `schema_version` 2-t (regressziós teszt: kézzel felépített v1 séma+sor, majd `Database(path:)` upgrade). Új `TagRecord` (`kind`/`target`/`sessionDate`/`tag`) + `Database.addTag/removeTag/tags(target:sessionDate:)/allTags()/targetsWithTag(_:)`; `kind` mindig `sessionDate` nil-ességéből származik (a hívó `kind`-jét eldobjuk), `addTag` idempotens explicit exists-check-kel (NEM `INSERT OR IGNORE` + `UNIQUE`, mert SQL `NULL` sosem egyenlő `NULL`-lal, így két target-szintű tag `session_date IS NULL` mellett nem ütközne a `UNIQUE(kind, target, session_date, tag)` indexen). CLI: `astrotool tag add/remove --target T [--date D] <tag> [--json]`, `tag list [--target T] [--date D] [--json]` (cél nélkül minden tag csoportosítva), `stats --tag TAG` szűrő (csak a target-szintű tag-gel rendelkező célpontok). `TargetStats`/`SessionDetail` új `tags: [String]` mezőt kapott (kompatibilis dekódolás: hiányzó `tags` kulcs `[]`-ra esik vissza egy kézzel írt `init(from:)`-ban). App: `AppState.addTag(target:date:tag:)`/`removeTag(...)` (háttérszálon fut, utána `stats`/`sessionDetails` frissül); `StatsView` táblát lenyitható (`DisclosureGroup`) célpont-sorokra cserélte — fejléc a régi oszlopokkal + tag-chipek, lenyitáskor lazy `loadSessionDetails`, session-önkénti chipek is; egyszerű `FlowLayout` (SwiftUI `Layout` protokoll) a chipek tördeléséhez, „+" chip popoverrel új címke felvételéhez, ✕ törléshez; a keresőmező most célpont NEVE VAGY címke szerint is szűr. |
| R3 Kalibráció hard-link | ✅ | 295 teszt zöld (281 + 14 új); ÚJ, fehérlistás írási művelet (spec 11.4, user-engedéllyel) — a session lightjaihoz/flatjaihoz/biaseihez illő `calibration_library/` mastereket **hard linkeli** a session saját `darks`/`biases` mappájába, meglévő fájlt SOHA nem ír felül. `WriteGuard.linkCalibrationFile(sourceRelative:destDirRelative:)`: forrás validáció `calibration_library/` alá (a `writeToolFile`-lal azonos `standardizedFileURL`-alapú traversal-védelem), cél validáció szigorúan `sessions/<target>/<date>/(darks\|biases\|flats)` mintára (komponensenkénti `.`/`..`/üres-szegmens tiltás + standardized-path containment defense-in-depth), dest mkdir -p, dest létezik → `nil` (skip), egyébként `FileManager.linkItem` (nincs csendes copy-fallback EXDEV-re). Új `Sources/AstroCore/Calib/CalibLinker.swift` (`CalibLinkPlan`/`CalibLinker.plan(target:date:db:config:)`/`apply(_:root:using:)`) — a `SessionMatcher.match` (session-leltár + library-dark fallback) és `CalibAnalyzer.matchedMasterDarkPath`/`parseMasterDirName` újrahasznosításával (nincs duplikált illesztési logika; a dominant-combo számítás kiemelve `CalibAnalyzer.dominantCombo(files:db:)`-ba, amit a `SessionMatcher` is mostantól ezen keresztül hív): darkok a lightoknak (csak ha a session-nek nincs saját darkja), flat-darkok a flatoknak (a flatok saját domináns exp/temp kombójából), bias masterek (csak ha a session-nek nincs saját biasa) — (forrás, cél) szerint dedupelve. CLI: `astrotool link-calib --target T --date D [--dry-run] [--yes] [--root R] [--json]` — emberi módban a teljes tervet kiírja és stdin-en kér megerősítést (`Type YES to link:`), kivéve ha `--yes`; `--json` módban `--dry-run` nélkül `--yes` kötelező (nincs stdin-blokkolás egy scriptelt hívásban); `--dry-run` sosem ír. App: „Kalibráció linkelése…" gomb minden session-soron (`StatsView` `SessionDetailPanel`) → `CalibLinkSheet` (terv célkönyvtár szerint csoportosítva, indoklással, „Linkelés" gomb üres tervnél letiltva „Nincs linkelhető kalibráció" helyszöveggel), sikeres linkelés után a `sessionDetails` frissül. |
| R3 Szemét-riport (cleanup) | ✅ | 307 teszt zöld (295 + 12 új); új `Sources/AstroCore/Audit/CleanupReport.swift` (`CleanupGroup`/`CleanupSummary`/`CleanupReport.build(db:config:maxPathsPerGroup:)`) — méret szerint csökkenő, kategóriánként aggregált „mit érdemes kitakarítani és mennyit nyerek vele" riport: reziduum-fájlok kiterjesztés/hely szerinti alkategóriákba sorolva (`residue-seq`/`residue-lst`/`residue-process-dir`/`residue-other`; egy `residueDirNames` alá eső fájl a saját nevétől függetlenül `residue-process-dir`, a `toolOutputDirNames` alá eső fájlok sosem számítanak reziduumnak), plusz egyetlen aggregált `duplicate-content` csoport (pazarolt bájt = méret × (n−1) csoportonként, a megtartott példány a `sessions/` másolat, ha van, egyébként az ábécé szerint első; ha még nincs `content_hash` a DB-ben, a csoport egyszerűen hiányzik, nem nullázva). Kiterjesztve `Rules.swift`-ből (`ResidueMatcher` megosztott helper, `ResidueRule` erre lett átállítva — nincs duplikált glob/dirName logika). `SuggestionScript` új `commentSuspicious: Bool = true` paramétere (`generate`/`write`) — alapból változatlan viselkedés, a cleanup script viszont `false`-t ad át, hogy a gyanús (`.suspicious`) találatok NE komment-sorként, hanem AKTÍV `mv`-ként kerüljenek a scriptbe. CLI: `astrotool cleanup [--root R] [--json] [--suggest] [--limit N]` — emberi kimenet méret szerint csökkenő csoportokkal + top útvonalak méretekkel + „összesen felszabadítható" összegzés; `--suggest` a `CleanupReport.quarantineFindings(for:timestamp:)` segítségével minden jelölt fájlt egy `.astro_tool/cleanup_quarantine/<timestamp>/<eredeti-relatív-útvonal>` alá mozgat (SOHA nem `rm`, a felhasználó a karanténmappát később kézzel ürítheti). App: `AppState.cleanupSummary`/`loadCleanup()`/`generateCleanupScript()` (audit futása után automatikusan frissül); `OverviewView` „Takarítás" doboz (összesen felszabadítható + top 3 kategória) és „Takarítási script generálása" gomb. |
| Audit UX (magyar üzenetek, csoportosítás) | ✅ | 315 teszt zöld (307 + 8 új); éles screenshotokból jött panaszok javítva. (A) Minden audit finding `message` (`Rules.swift` mind a 15 szabálya, `DuplicateFinder`, `SessionMatcher`, `CleanupReport`) magyarra fordítva — az útvonalak/technikai értékek interpolálva maradtak; a `missing-counterpart` teszt lazítva kategória+útvonal-ellenőrzésre (a szöveg még alakulhat), 2 explicit magyar spot-check maradt (`.DS_Store` reziduum, FITS IMAGETYP üzenet). (B) Új `AuditEngine.suppressRedundantFindings` post-pass: minden szabály lefutása után eldobja azokat a `calib-in-wrong-dir`/`misplaced-file`/`loose-frames-in-date-dir` találatokat, amiknek az útvonala egy ugyanebben a futásban `nested-session-tree`-vel jelölt mappa alatt van — egy beágyazott session-fa (pl. `sessions/<target>/<date>/flats/sessions/session1/darks/`) így egyetlen `nested-session-tree` találatként jelenik meg a mögötte lévő tucatnyi/százas nagyságrendű azonos per-fájl sor helyett (a `PathClassifier` a role-t a rögzített 4. path-komponensből olvassa, ezért minden beágyazott fájl ugyanazt a hibás role-t kapja → korábban tömeges duplikált `calib-in-wrong-dir`). (C) `AstroConfig.toolOutputDirNames` új alapértelmezett eleme: `"masters"` (stackelt master fájlok szándékos `masters/` alkönyvtára a raw-ok mellett) — `CalibInWrongDirRule.misplacedFinding` (és a megosztott `SessionMatcher` hívása) mostantól kihagyja a bármely tool-output mappa alatti fájlokat, a `ToolOutputRule` egy `probablyIntentional` találatot ad a `masters/` mappára. (D) Új `Sources/AstroCore/Audit/FindingGrouper.swift` (`groupKey(for:config:)`/`group(_:config:)`) — megosztott csoportosító logika: `residue`-nál fájlnév-minta osztály (`.DS_Store` vagy `*.seq`), `duplicate-content`/`calib-in-wrong-dir`/`misplaced-file`-nél szülő-könyvtár, minden más (mappa-szintű) kategóriánál a saját útvonal; súlyosság szerint, majd csoportméret szerint csökkenően rendezve. `AuditView` (app) a lapos táblázatot lenyitható (`DisclosureGroup`, a Stats fül stílusában) csoport-listára cserélte: súlyosság-ikon + kategória + csoportkulcs (középen csonkolva) + „N fájl" jelvény + első üzenet összefoglalóként, lenyitva az egyedi találatok (útvonal+üzenet) lazán épülnek fel; a súlyosság Picker + kategória-szűrő megmaradt a csoportokon. (E) CLI `astrotool audit` emberi kimenete (`Commands.swift` `printAuditFindings`) ugyanezt a `FindingGrouper`-t használja: csoportonként egy összegző sor a darabszámmal, majd max 3 példa-útvonal behúzva — a teljes részletezés a `--json` módban változatlan. Új teszt: `Tests/AstroCoreTests/FindingGrouperTests.swift` (6), `AuditTests.swift` +2 (`auditSuppressesCalibInWrongDirUnderNestedSessionTree`, `auditSkipsCalibInWrongDirUnderMastersToolOutputDir`), `ConfigTests` frissítve a `masters` bejegyzésre. |
| Statisztika táblázat újratervezés (hierarchikus Table) | ✅ | 315 teszt zöld (UI-hoz nincs unit teszt, core változatlan); a `DisclosureGroup`-alapú lista lecserélve natív SwiftUI hierarchikus `Table`-re (`Table(_:children:columns:)`, macOS 14) — oszlopfejlécek, átméretezhető oszlopok, váltakozó sorháttér, beépített lenyitó-chevron a session-sorokért, minden érték oszlopba rendezve (a session-sorok automatikusan behúzva az első oszlopban). Új view-réteg `StatsRow` (`Sources/AstroToolApp/Views/StatsView.swift`): `.target(TargetStats)` / `.session(target:detail:)` eset, `children: [StatsRow]?` (nil = nincs lenyitó-chevron). Betöltési megoldás: mivel a `Table(children:)` nem tud lazy-lenyitáskor betölteni, `AppState.loadStats()` mostantól minden célponthoz egyszerre lekéri a session-részleteket is (`sessionDetailsByTarget: [String: [SessionDetail]]`, egy `SessionStatsQueries.sessions` hívás célpontonként, ugyanazon háttérműveleten) — a régi egy-célpontos `loadSessionDetails`/`sessionDetails`/`selectedTarget` API-t eltávolítottuk (ellenőrizve: sehol máshol nem hívták). 8 oszlop: Célpont/Session (behúzással + wide-field jelvény + README badge), Integráció, Keretek (session: nem-nulla szerepek „N L · N F"-ként), Expozíciók/Utolsó dátum (session: „60s×180, ?×203" — `?` az ismeretlen expozíciónak), Kamera, Részletek (csak session-sorok: gyújtótáv/gain/hőm./szűrő, csak a meglévő részek), Címkék (tag-chipek + „+" popover, változatlan `AppState.addTag/removeTag`), Műveletek (session: „Kalibráció linkelése…" gomb, sor-szkópolt `LinkingSession` sheet-item). A tag-hozzáadás/törlés és a kalibráció-linkelés utáni frissítés mostantól csak az érintett célpont `sessionDetailsByTarget` bejegyzését cseréli le (nem az egészet). |
| Crash-javítás (FITSReader slice-index): ✅ | ✅ | 318 teszt zöld (315 + 3 új); éles `.ips` crash (v0.2.2, „Pontozás" a Minőség fülön) gyökéroka: egy fájlban véletlenül szomszédos CR+LF bájtpár (`0x0D 0x0A`) a fejléc-blokkban — Swift a `"\r\n"`-t EGYETLEN `Character`-ként grafémaklaszterezi (Unicode UAX #29 GB3 „ne törj CR és LF között"), így egy 2880 bájtos blokkból `Array(String(data:encoding:.ascii))` csak 2879 elemű tömböt adott. A `FITSReader.readOneHeader` (és a testvér `NativeStats.primaryHeaderInfo`, ami szándékosan duplikálja ugyanezt a blokk-szkennelést a nyers primary NAXIS miatt) 0-alapú, fix `cardIndex * 80` szeletelése emiatt „Array index is out of range"-dzsel elszállt, amint a ciklus elért egy olyan kártyáig, aminek a tartománya már nem fért bele a lerövidült tömbbe — pontosan a crash-riport `readOneHeader` closure EXC_BREAKPOINT-ja. Reprodukálva: két új teszt (`crLfBytePairInFirstBlockOfMultiBlockHeaderDoesNotCrashParse` a `FITSReaderTests.swift`-ben, `nativeStatsCrLfBytePairInFirstBlockOfMultiBlockHeaderDoesNotCrash` a `RateTests.swift`-ben) fixfix-előtt ténylegesen elszállt (`Fatal error: Array index is out of range`, signal 5) — verifikálva a javítás előtt futtatva. Javítás: mindkét helyen bájtonkénti dekódolás (`data.map { Character(Unicode.Scalar($0)) }`) a `String(data:encoding:.ascii)` + `Array(_:)` pár helyett — szigorú 1:1 bájt↔tömbelem megfelelés, függetlenül a bájttartalomtól (a nem-ASCII ellenőrzés `blockData.allSatisfy({ $0 < 0x80 })`-ra váltva). Az `autoreleasepool` memóriakorlátozás változatlan. Harmadik biztonsági háló teszt: `crLfBytePairFamilySweepAcrossPositionsAndHeaderShapesNeverTraps` — a CR+LF párt végigsöpri több fejléc-alakzaton (egyblokkos END az utolsó kártyán, többblokkos END nélkül az első blokkban, `.fz`-stílusú kétfejléces merge) és sok pozíción, sosem szabad elszállnia. `Rater.rate` már meglévő `do/catch` blokkja (`NativeStats.compute(url:)` körül, `continue`-val) változatlanul véd — a valódi védelem a forrás-javítás, mert a `Fatal error` trap sosem catch-elhető, csak a most bevezetett, kizárólag dobható-hibás útvonal. |
| Minőség fül bővítése (részletes táblázat) | ✅ | 320 teszt zöld (318 + 2 új); a felhasználói panasz szerint a Minőség fül csak Útvonal/Pontszám/Kiugró oszlopokat mutatott — ha a Siril nem adott metrikát egy kerethez, a pontszám kizárólag a háttér-metrikából jött, ezért azonos-pontszám klaszterek jelentek meg, és a hosszú útvonalakból nem látszott, melyik saját (pl. `lights/Junk`) triázs-almappában ül a keret. `FrameScore` (`Sources/AstroCore/Rate/Rater.swift`) additív bővítése: `saturatedFraction: Double?`, `exptime: Double?` (a scoring exptime-csoport kulcsából), számolt `fileName` (nem tárolt), és `sessionSubdir: String?` (a `sessions/<target>/<date>/` és a fájlnév közötti útvonal-rész, pl. `"lights/Junk"`, `nil` ha a keret közvetlenül a dátum-mappában ül) — mind opcionális, ezért a szintetizált `Codable` `decodeIfPresent`-tel dekódol, a régi (mező nélküli) JSON változatlanul betölthető marad. `Rater.scoreGroup` tölti fel mindet (a `RatingRecord.saturatedFraction`-ből, a csoportosításhoz már meglévő exptime-ból, és egy új `Rater.sessionSubdir(path:)` helperből). `QualityView` (app): 10 oszlopos `Table` (Fájl + `.help(teljes útvonal)` tooltip, Mappa, Pontszám monospacedDigit, FWHM, Kerekség, Csillagok, Háttér, Szat. %, Exp., Kiugró), sortOrder-alapú rendezés (Pontszám natívan `KeyPathComparator`-ral, a többi opcionális metrikának nem-opcionális sort-surrogate mező, mert a `TableColumn(value:)` automatikus rendezhetősége `Optional`-ra nem old fel — lásd lentebb), kiugró sorok piros szövegszínnel, futás utáni összegző sor ("N frame · kiugró: K · Siril metrika: M/N", ha M==0 és a Siril elérhető lenne, figyelmeztető megjegyzéssel). CLI `rate` emberi táblázata (`Commands.swift` `printRateTable`) új FWHM/Kerekség/Csillagok/Háttér/Szat.% oszlopokkal ("-" nil esetén); `--json` a `Codable` bővítés miatt automatikusan hozza az új mezőket. Megjegyzés a jövőnek: a SwiftUI `Table`'s `TableColumn(_:value:content:)` overloadja `Optional<Double>`/`Optional<String>` keypath-re NEM ad `some View`-t visszaadó, sortolható változatot (csak egy szűkebb, kizárólag pontos `Text`-et visszaadó, ráadásul a helyi SDK-n ettől is "no exact matches"-szel elszálló overload létezik) — ezért minden opcionális metrikának van egy nem-opcionális `...SortKey` surrogate property-je (nil → `-.infinity`/`.min`/`""`), amit a `TableColumn(value:)` lát, míg a cellatartalom a valódi opcionális értéket formázza. |
| R4-1 Valós integráció (inode-dedup, szerep-szűrés, `_hibas` kizárás) | ✅ | 350 teszt zöld (320 + 30 új); az asztrofotós szakértői review (`docs/superpowers/reviews/2026-08-03-astrophotographer-review.md`) szerint a kimutatott integráció ~30%-kal felfújt volt (42.55h a valós 29.77h helyett) négy ok miatt: (1) a Siril-oldali LightFrameRater `Stack`/`Review`/`Reject/<ok>` triázs-almappái ugyanannak a framenek hardlinkjeit hozzák létre `lights/` alatt, mindegyik külön light-ként számolva; (2) CR3+TIF pár (ugyanaz a DSLR-keret nyersen és konvertálva) duplán számolt; (3) a `PathClassifier` MINDENT role `.light`-nak jelöl a `lights/` alatt, így `.xmp`/`.png`/`.txt`/`.html`/`.csv`/`.ssf`/`.json` sidecar-ok és feldolgozott derivatívumok (`starless_*`, `starmask_*` .fit, ezek `FILTER` fejléce is szennyezte a szűrő-listát) is számítottak; (4) a `Reject/`-be triázsolt keretek is "megvan"-ként számoltak; (5) a `_hibas`-címkés session-ök (a felhasználó saját "rossz éjszaka" jelölése) teljesen beleszámítottak a célpont-összegekbe. Séma v3 (`Database.schemaSQLv3`, verzió-lépcsős migráció mint v2): `files.inode`/`files.nlink INTEGER` (nullable) — `Scanner` minden új/módosult fájlnál `FileManager.attributesOfItem` `.systemFileNumber`/`.referenceCount`-tal tölti, az unchanged-heal ágban CSAK akkor stat-ol újra, ha `inode` még NULL (nincs felesleges churn); `Database.backfillInode(id:inode:nlink:)` célzott UPDATE, nem érinti a többi oszlopot. Új `Sources/AstroCore/Stats/FrameSet.swift` (`FrameBuckets`/`FrameSet.lightBuckets(files:meta:config:)`) — a "melyik fájl valódi, használható light keret" egyetlen igazságforrása: nem-keret kiterjesztés vagy derivatívum-név → `nonFrameFileCount`; dedup elsődlegesen `inode` szerint (a megtartott példány az, aminek a szülő könyvtára szó szerint `lights`, nem egy `Stack`/`Review`/`Reject` triázs-almappa), `inode` hiányában `(target, sessionDate, csak-számjegyekre normalizált DATE-OBS, exptime)` fallback-kulccsal, majd cross-extension CR3+TIF összevonás (azonos normalizált DATE-OBS vagy fájlnév-törzs alapján, a nyers CR3 marad); a dedup után `Reject/` alatti példányok külön `rejected` kosárba kerülnek. `AstroConfig.stats: StatsRule` (`excludeLabels: ["hibas"]` alapértelmezett, `gapThresholdSeconds: 0` — ez utóbbi R4-2-nek fenntartva, még nincs bekötve) — `.labeled` kind session-dátumok, ha a címkéjük (kissebb-nagybetű-független) szerepel az `excludeLabels`-ben, kimaradnak a célpont-összegekből. `StatsQueries`/`SessionStatsQueries` mindkettő `FrameSet`-en megy át: `TargetStats` additív mezői (`usableIntegrationSeconds`, `grossIntegrationSeconds` — a RÉGI, dedup nélküli összeg —, `usableFrameCount`, `duplicateLinkCount`, `rejectedFrameCount`, `nonFrameFileCount`, `excludedSessionDates`), `totalIntegrationSeconds` mostantól EGYENLŐ a usable-lel (a fejléc-szám igaz kell legyen); `SessionDetail` additív mezői (`usableLightCount`, `rejectedCount`, `duplicateLinkCount`, `isExcludedFromTotals`) — a session-részletek a saját valós számaikkal jelennek meg akkor is, ha ki vannak zárva a célpont-összegzésből. Tiszta (dupla/nem-keret nélküli) fixture-ökön a régi tesztek változtatás nélkül zöldek maradtak (usable == gross ott); új tesztek: hardlinkelt pár egyszer számít (valódi hardlink `FileManager.linkItem` a tmp fixture-ben), CR3+TIF pár egyszer számít, `.xmp` nem számít fényként, `Reject/`-beli keret a rejected kosárban landol (nem usable), `_hibas` session kizárva a célpont-összegből de jelen van a session-listában `isExcludedFromTotals` jelzéssel. CLI: `stats` emberi kimenete a usable-t mutatja fő számként, `--gross` kapcsoló mellé teszi a bruttó oszlopot/sort is, `--json` mindent visz. App: `StatsView` Integráció oszlopa változatlanul a (mostantól usable) `totalIntegrationSeconds`-t mutatja, session-sor Keretek cellája `usableLightCount`-ot ír "N light" formában + "(+N elvetett · N link)" toldalékkal ha nemnulla, kizárt (`_hibas`) session-sor fél-áttetsző + piros "kizárva" chip, célpont-sor név-cellája `.help()` tooltippel a teljes bontásra. |
| R4-2 Abszolút session-minőség (arcsec, e-/s), éjszaka-idővonal, rate-javítások | ✅ | 376 teszt zöld (350 + 26 új); séma v4 (`Database.schemaSQLv4`): `fits_meta.xpixsz`/`egain` új oszlopok, migráció visszatölti a meglévő sorok `header_json`-jából (nincs fájlolvasás — regressziós teszt: kézzel épített v3 séma+sor `header_json`-nal, majd `Database(path:)` upgrade), `Scanner.fitsMetaRecord` új FITS-eknél a fejlécből (`XPIXSZ`/`EGAIN`) rögzíti. Új `Sources/AstroCore/Stats/SessionQuality.swift` (`SessionQualitySummary`/`SessionQuality.summaries(target:db:config:)`) — session-önkénti ABSZOLÚT (beállítás-független, nem z-score) metrikák a `ratings`⋈`fits_meta` join-ból: medián FWHM px-ben és ívmásodpercben (`pixelScaleArcsec = 206.265 × xpixsz(µm) / focallen(mm)` — valós adaton 3.76µm/302mm → 2.569"/px, tesztelve 0.01 tűréssel), égi háttér e-/s/ívmásodperc²-ben (`háttér(ADU) × egain / exptime / skála²`), medián csillagszám, kiugró-arány (a tárolt `score` és `config.rating.outlierZScore` alapján, nem a `Rater` újrafuttatásával), és rangsor a célpont session-jei között (1 = legjobb/legkisebb ívmásodperces FWHM; hiányzó metrikájú session `rankAmongSessions == nil`, nem az utolsó rangot kapja). Új `Sources/AstroCore/Stats/SessionTimeline.swift` (`SessionTimeline`/`SessionTimeline.timeline(target:date:db:config:)`) — éjszaka-idővonal a használható (dedupolt, `FrameSet`-en átment) lightok DATE-OBS-jából: ablak eleje/vége (ISO 8601 UTC), integráció, hatékonyság (integráció/ablak), csendes kiesések listája (`config.stats.gapThresholdSeconds` explicit érték, VAGY 0 esetén auto: 3× a session medián NOMINÁLIS expozíciója); mind a FITS (`2026-04-18T04:36:24.123`) mind az EXIF (`2026:04:18 04:36:24`) DATE-OBS formátumot érti, időzóna-mentes UTC feltevéssel. Rate-javítások (asztrofotós review nyomán): (a) új `Sources/AstroCore/Stats/NominalExposure.swift` (`NominalExposure.nominal(_:)`) — egy valós könyvtárban az exptime lebegőpontos zajjal jár (pl. 30.0×822 és 29.899999618523×91 ugyanaz a "30s" sub, hasonlóan 120.0/119.9, 360.0/359.7) — 10s alatt 0.1s-re, 10s-től egész másodpercre kerekítve, ez a KÖZÖS helper mindenhol: a `Rater` pontozás-csoportosításában, a `StatsQueries`/`SessionStatsQueries` `exposureBreakdown` kulcsaiban, és a `SessionTimeline` auto gap-küszöbének medián-számításában; (b) `Rater.ExposureGroupKey` mostantól `(sessionDate, nominális exptime)` párra kulcsol (nem csak exptime-ra) — egy `--date` nélküli, több éjszakát átfogó `rate` így nem keveri össze a különböző éjszakák égbolt-viszonyait egy z-score populációba; (c) `SirilCLI.parseFindstarOutput` a hiányzó kerekségre mostantól `nil`-t ad vissza a korábbi kitalált `0.5` helyett (`StarMetrics.roundness` `Double` → `Double?`, `Rater`'s metrika-összeállítása már nem követeli meg a kerekséget a `fwhm`/`starCount` mellett) — a `Rater` súly-újranormalizálása változtatás nélkül kezeli a hiányzó metrikát (meglévő teszt verifikálva). CLI: `astrotool quality --target T [--date D] [--json]` (dátum, keret, FWHM px/", háttér e-/s/"², csillag, kiugró%, rang táblázat) és `astrotool stats --target T --timeline [--date D] [--json]` (ablak, integráció, hatékonyság%, kiesés-lista); mindkettő `--target` nélkül exit 1. App: `QualityView`-ban a keret-táblázat FÖLÉ kerül egy session-összegző szakasz (dátum · keret · FWHM" · háttér · rang jelvény pl. "2/6"), session-sorra kattintva idővonal-sor jelenik meg ("Ablak 3:42 · integráció 2:11 · hatékonyság 59% · 2 kiesés (37m, 12m)") — `AppState.loadQualitySummaries(target:)`/`loadSessionTimeline(target:date:)` új háttérműveletek, target-váltáskor (`onChange`) automatikusan újratöltve. Új tesztek: `DatabaseTests` (v3→v4 migráció + header_json backfill), `ScannerTests` (xpixsz/egain új FITS-ből), `SessionQualityTests.swift` (arcsec/háttér matek pontosan, medián páros/páratlan elemszámmal, kiugró-arány, rangsor nil-kezeléssel, kecses nil-ek), `SessionTimelineTests.swift` (ablak/kiesés/hatékonyság matek, explicit és auto gap-küszöb, vegyes DATE-OBS formátum, egyetlen keret, ismeretlen session), `RateTests` (float-zajos exptime egy csoportba, több-éjszakás pontozás session-önkénti csoportosítással, `parseFindstarOutput` nil kerekség), `StatsTests`/`SessionStatsTests` (exposureBreakdown kulcs-összevonás), `CLISmokeTests` (`quality` + `stats --timeline` JSON/emberi/hiba-eset). |
| R4-3 Kalibráció-illesztés teljes kulccsal (gain/offset/bin/kamera), DATE-OBS-alapú master-kor | ✅ | 385 teszt zöld (376 + 9 új); asztrofotós szakértői indíttatás: a `CalibAnalyzer` eddig csak a master DIR NÉV-ből olvasott (exp, temp) alapján párosított darkot — de az ASI2600 (és minden hűtött CMOS) dark-jele GAIN-től és OFFSET-től is függ, egy gain-0 dark gain-100 lightokra linkelve AKTÍVAN ÁRT (rossz zajprofil). **KÉT ALAPÉRTÉK-VÁLTOZÁS (meglévő `config.json`-t mentő felhasználók a RÉGI értéküket tartják meg — a Beállítások "Mentés" gombja a jelenlegi, nem az új alapértékeket írja ki)**: `tempToleranceC` 0.5→**1.0** (egy hűtött CMOS set-pontja ±0.1-0.2°C-ot ingadozik, a dark-áram ~5-6°C-onként duplázódik, a régi érték feleslegesen szigorú volt), `darkMaxAgeMonths` 6→**12** (a kor csak FIGYELMEZTETÉS, nem elsődleges érvénytelenítő — az elektronikus kulcs az). `AstroConfig.calib` additív mezői: `matchGain`/`matchOffset`/`matchBinning`/`matchCamera: Bool = true`, `gainTolerance: Double = 0`, `exposureToleranceFraction: Double = 0.02` (relatív tűrés, az abszolút `exposureToleranceS`-hez ADVA — 29.9s light illeszkedik egy 30s masterre). `CalibAnalyzer`: `CalibCombo` mostantól (exp, temp, gain, offset, camera) — gain/offset a light `fits_meta` oszlopaiból (ingyenes, már ott vannak), camera az `INSTRUME`-ból; a masterek saját aggregátumot kapnak (`MasterDir`: dominant gain/offset/instrume a `fits_meta` oszlopokból, dominant `xbinning` a `header_json`-ból parse-olva — ez utóbbi TUDATOSAN csak a masterekre, mert azokból száz van, a lightokból ezrek lehetnek, per-light XBINNING-parse-olás nem éri meg; `matchBinning` így ma nem tud ténylegesen elutasítani, csak elő van készítve egy jövőbeli per-light binning-capture-höz). Kor: a dir legújabb `DATE-OBS`-ja (FITS/EXIF formátum, `SessionTimeline.parseDateObs` megosztva), fájlonkénti mtime-fallback ha nincs DATE-OBS — egy sima copy/rsync visszaállítja az mtime-ot, de a DATE-OBS-t nem, ez az igazi kor-forrás. Illesztés (`findMatch`/`electronicMismatchReasons`): a (exp, temp) tűrésen belüli "coarse" jelöltek közül csak az illeszkedik teljesen, ami MINDEN bekapcsolt dimenzión egyezik (gain `gainTolerance`-en belül, offset/camera/binning egzakt) — offset/binning/camera csak akkor bukik el, ha MINDKÉT oldalon van érték (hiányzó adat sosem hibáztat). Ha volt coarse jelölt, de egyik sem illeszkedett elektronikusan: `matchedMasterPath` marad `nil`, de `CalibNeed.mismatchReasons` (magyar, pl. `"gain 0 ≠ 100"`, `"másik kamera: ZWO ASI2600MC Pro"`) és `requiredGain`/`requiredCamera` kitöltve. `matchedMasterDarkPath` publikus aláírása bővült (`gain`/`offset`/`camera`/`db` paraméterek, `throws`, visszatérése `(path, mismatchReasons)`) — `SessionMatcher.dominantLibraryDark` (`SessionCalibration.libraryDarkMismatchReasons` új mező) és `CalibLinker.plan` (flat-dark ág) ugyanazt az utat hívja, nincs duplikált illesztési logika. `CalibLinkPlan` additív `mismatchReasons: [String]` mező — ha az EGYETLEN (exp,temp)-jelölt masterből elektronikailag bukik a light-dark VAGY a flat-dark, az üres terv ("nincs link") ezt is hordozza, nem csak csendben üres. CLI: `calib` emberi kimenete a todo alá `⚠️ nem illeszkedő master: ...` sort ír ha van mismatch, `link-calib` üres terv esetén `"nem linkelhető: ..."`-t ír a `"nincs linkelhető kalibráció"` helyett ha van ok. App: `CalibrationView` táblázat új "Megjegyzés" oszlopa (`mismatchReasons` összefűzve), `CalibLinkSheet` üres terv esetén a reasons-t mutatja a semleges üzenet helyett. Új tesztek (`CalibTests.swift`/`CalibLinkerTests.swift`): header-aggregáció (GAIN/OFFSET/INSTRUME/XBINNING fixture-fejlécből) + mindkét ok szövege, gain-mismatch elutasítás+ok, `matchGain=false` elfogadás, offset csak mindkét oldalon aktív, relatív exp.-tűrés (29.9s↔30s), új 1.0°C temp-alapérték, kor DATE-OBS-ból (mtime=most, date_obs=400 napja → stale a 12 hónapos alapértékkel), DSLR light (ISO a gain oszlopban) NEM illeszkedik ASI masterre kamera-eltérés miatt, `CalibLinker` üres terv + reasons elektronikus mismatch esetén; a régi `darkCoverageSortsMissingBeforeStaleBeforeCovered` teszt explicit `darkMaxAgeMonths = 6`-ra van pinnelve (a sorrend-tesztje független az alapértéktől). |
| R5-1 Észlelés-tervező (kulmináció, Hold, láthatósági ablak, `plan` parancs) | ✅ | 443 teszt zöld (385 + 58 új); a könyvtár-kezelőt éjszakai tervezővé bővítő feature. Új, zéró-függőségű `Sources/AstroCore/Sky/` modul: `JulianDate` (UTC→JD, J2000.0 pontos egyezés `2451545.0`-lel), `SiderealTime` (IAU 1982 GMST-polinom, Meeus *Astronomical Algorithms* 12.a/12.b mintapéldáival bitre egyező validálva: 1987-04-10T00:00Z→13h10m46.3668s, 19:21:00Z→8h34m57.0896s; a feladatban javasolt 2004-04-06T00:00Z „~12h59m32.3s" referenciaérték SAJÁT, független újraszámolással kb. 63 másodperccel eltért a validált formulától — mivel a formula bitre stimmel a Meeus-tankönyv két hivatalos példáján, ez a felhasználó emlékezetből írt referenciaértékének pontatlansága, nem formulahiba; a kettő-implementációs kereszt-ellenőrzés (poly vs JD0h+eltelt-idő, 0.1s tűréssel) 2004-04-06-ra és egy 2026-os epochra is lefut), `AltAz` (ekvatoriális→horizontális + légtömeg, zenit-áthaladás/Sarkcsillag/szimmetrikus óraszög tesztekkel), `SunMoon` (Meeus alacsony-pontosságú Nap-pozíció + csonkolt Hold-sorozat a Meeus 47.A/47.B táblák ~25 legnagyobb tagjával, Hold-fázis a `(1-cos elongáció)/2` közelítéssel, csillagászati szürkület bisection-szkenneléssel −18°-ra, nyári sarkvidéki „nincs csillagászati éjszaka" esetén −12°-ra visszaeső jelzéssel). A Hold új/telihold-teszteket a FELADAT SAJÁT javaslata szerint nem a megadott naptári dátumokból, hanem a fázisformula saját közép-újhold k-ciklus képletéből (Meeus 49.1) számolt pillanatokból ellenőrzi (>97%/<3% küszöbbel) — ez a k-ciklus-számítás a felhasználó által említett 2026-08-13/2026-08-28 dátumokat gyakorlatilag napra pontosan visszaadta, megerősítve mind a Hold-, mind a Nap-formulát. Új `Sources/AstroCore/Sky/TargetCoordinates.swift`: célpont RA/Dec mediánja a session lightok `header_json`-jából (plate-solve `CRVAL1`/`CRVAL2` elsőbbséggel, utána numerikus vagy szexagezimális `RA`/`DEC` fallback — RA óra→fok szorzás KIZÁRÓLAG a szexagezimális formánál), helyszín-medián a `SITELAT`/`SITELONG` fejlécekből. `AstroConfig.site: SiteRule` (`latitudeDeg`/`longitudeDeg: Double?`, alapból `nil`) — ha üres, `Planner.resolveSite`/`AppState.loadPlan()` a könyvtár-mediánt számolja és CSAK memóriában cachelja (sosem ír lemezre). Új `Sources/AstroCore/Sky/Planner.swift` (`TargetPlan`/`Planner.plan(date:minAltitudeDeg:db:config:)`): célpontonként megvan/cél (a meglévő `goal:<óra>h` tag-konvenció lezser parse-olással, pl. `goal:6.5h`) órák, ma esti kulmináció + max magasság (2 perces brute-force szkennelés a szürkület-hajnal ablakon, egyszerűbb és megbízhatóbb, mint a tranzitidő zárt alakú megoldása), láthatósági ablak `--min-alt` fölött, Hold fázis% és szögtávolság, magyar verdikt (`"ma jó"` / `"Hold zavar (N°, M%)"` / `"alacsony (max N°)"` / `"nem látszik ma éjjel"` / `"nincs koordináta"`) és pontszám (hiány×láthatóság×Hold-büntetés) szerinti csökkenő rendezés. CLI: `astrotool plan [--date YYYY-MM-DD] [--min-alt 30] [--json]` — az emberi fejléc a szürkület/hajnal időt és a Hold fázisát mutatja, de SOHA a helyszín koordinátáit (csak a `config show` helyi parancs mutathatja); táblázat CÉLPONT/MEGVAN/CÉL/KULMINÁCIÓ/MAX ALT/ABLAK/HOLD/VERDIKT oszlopokkal. App: „Ma este" doboz az Áttekintés fülön (top 5 célpont színezett verdikt-jelvénnyel, „Frissítés" gomb → `AppState.loadPlan()`, a feloldott helyszínt memóriában visszacacheli a `config.site`-ba). Új tesztfájlok: `JulianDateTests`, `SiderealTimeTests`, `AltAzTests`, `SunMoonTests`, `TargetCoordinatesTests`, `PlannerTests` (60 új teszt a Sky matekra/tervezőre), `ConfigTests`/`CLISmokeTests` bővítve (`SiteRule` partial-decode, `plan` JSON/emberi/hibaeset/min-alt smoke tesztek, expliciten ellenőrizve hogy a helyi koordináták SOHA nem jelennek meg az emberi kimenetben). |
| R5-2 Projekt-státusz (pipeline-állapot célpontonként, `projects` parancs) | ✅ | 454 teszt zöld (443 + 11 új); a "felhős este mit dolgozzak fel?" feature. Új `Sources/AstroCore/Stats/ProjectStatus.swift` (`ProjectPhase`: `gyujtes`/`stackelheto`/`feldolgozasra_var`/`kesz`, `ProjectState`, `ProjectStatusQueries.projects(db:config:)`) — célpontonként a `StatsQueries.perTarget` + `SessionStatsQueries.sessions` eredményére és a `files` tábla `stacks`/`processed` soraira épül: a `sessions`/`stacks`/`processed` dátum-mappák `SessionDateParser`-rel felbontott (start,end) span-jai közötti átfedés-teszt ugyanaz a logika, mint az audit `MissingCounterpartRule`-jáé (nem duplikálva, csak a saját, egyszerűbb helyi implementációjában újraírva, mert az audit `AuditContext.directories`-ra épül, a `ProjectStatusQueries` viszont `db.allFiles()`-ra). A `goal:<óra>h` cél-tag parse-olása kiemelve a `Planner`-ből egy megosztott `Sources/AstroCore/Stats/GoalTag.swift`-ba (`GoalTag.parse(tags:) -> Double?`) — a `Planner` már ezt hívja, nincs duplikált logika. `AstroConfig.stats.collectingThresholdSeconds` additív mező (alapból 7200s = 2 óra): stack nélküli, e küszöb alatti célpont "gyűjtés"-nek számít akkor is, ha nincs cél-tag. Fázis-prioritás: `gyujtes` > `stackelheto` > `feldolgozasra_var` > `kesz`; a `_hibas`-címkés (kizárt) session-ök a `latestSessionDate`/fázis-számításból kimaradnak (egy kizárt session dátuma sosem húzza vissza a célpontot `feldolgozasra_var`-ból `stackelheto`-ba), de saját to-do sorban ("kizárt session: dátum (hibas)") megjelennek. To-do sorok pontos szövege: "készíts stacket: cél/dátum" (session-önkénti, ha nincs rá stack a dátumán vagy azutánról), "dolgozd fel: stacks/cél/dátum" (stack-önkénti, ha nincs rá processed), "hiányzik még N.N óra a célhoz (goal:Xh)" (a cél-tag eredeti egész/tört óraszámával, nem a parse-olt másodperccel), "nincs README: cél/dátum" (usable session README nélkül), "kizárt session: dátum (label)". Rendezés: minden nem-`kesz` fázis a `kesz` előtt, azon belül `missingSeconds` csökkenő (cél nélküli célpont a lista végén a csoporton belül), majd névsor. CLI: `astrotool projects [--root R] [--json]` — emberi kimenet fázisonként csoportosítva magyar fejlécekkel ("Gyűjtés alatt"/"Stackelhető"/"Feldolgozásra vár"/"Kész"), célpontonként név + meglévő/cél óraszám + első 2 to-do behúzva. App: `AppState.projectStates`/`loadProjects()` (scan után automatikusan frissül, `runScan()`-ban a Stats/Calib mellett), `OverviewView` új "Projektek" doboza a Takarítás alatt — fázisonkénti darabszám színes jelvényekkel, top 3 nem-kész célpont az első to-dójukkal. Új tesztfájl `Tests/AstroCoreTests/ProjectStatusTests.swift` (9: mind a 4 fázis elérhető, cél-tag hiány/meglét vezérli a `gyujtes`-t, `_hibas` kizárás fázisban de todo-ban jelen van, to-do szövegek pontosan egy-egy forgatókönyvre, rendezési sorrend), `CLISmokeTests` +2 (JSON dekódolás + fázis, emberi fejlécek), `ConfigTests` bővítve a `collectingThresholdSeconds` alapértékre/partial-decode-ra. |
| R5-3 Acquisition export (AstroBin CSV, generic CSV, Markdown session-napló, `export` parancs) | ✅ | 468 teszt zöld (454 + 14 új); a "célpont publikálásához 10-15 perc kézi adatgyűjtés" fájdalompontra egyetlen paranccsal/gombbal előállítja a TRUE (dedupolt) számokból az acquisition-riportot. Új `Sources/AstroCore/Export/AcquisitionExport.swift` (`ExportFormat`: `astrobin`/`csv`/`md`, `AcquisitionExport.render(target:format:db:config:)`/`write(target:format:timestamp:db:config:using:)`) — mindhárom formátum kizárólag meglévő lekérdezésekre épül (`SessionStatsQueries`, `SessionQuality`, `SessionTimeline`, `StatsQueries`, `GoalTag`), a `FrameSet`-szemantika (dedup, `_hibas` kizárás) ingyen jön velük. `astrobin`: AstroBin "long acquisition" bulk-import CSV-je, pontos fejléc `date,filter,number,duration,binning,gain,sensorCooling,darks,flats,flatDarks,bias,bortle,meanSqm`, egy sor session×nominális-expozíció csoportonként (dátum = `SessionDate.start`, szám/időtartam a csoport keretszámából/nominális exptime-jából, szűrő/gain a session domináns értéke, sensorCooling a `set_temp`/`ccd_temp` medián egész számra kerekítve — DSLR-nél üres —, darks/flats/bias a session saját (nem dedupolt) kalibráció-számai, flatDarks a flatek domináns nominális exptime-jával egyező dark-ok száma; binning MINDIG üres, mert a light-oldali binning ma nincs elmentve — lásd `CalibRule.matchBinning` doksi —, nem szabad 1-et tippelni; bortle/meanSqm üres, jövőbeli README-indexelésnek fenntartva). `_hibas`-kizárt session teljesen kimarad (helytelenül jelentené a publikált integrációt). `csv`: gazdagabb, általános session-önkénti CSV (`target,date,frames_usable,frames_rejected,exposure_s,integration_s,camera,gain_iso,sensor_temp_c,focal_length_mm,filters,fwhm_arcsec_median,background_e_s_arcsec2,tags`) — a `SessionQualitySummary`-vel dátum szerint joinolva, `_hibas` session is szerepel benne (ez egy teljes napló, nem publikálásra kész lista); szabványos CSV-escaping (vessző/idézőjel/újsor esetén idézőjelbe téve, belső idézőjel duplázva). `md`: emberi session-napló magyar címkékkel — célpont `#` fejléc, session-önkénti `##` alszakasz (README/keretek/expozíciók/kamera-optika/minőség-sor/idővonal-sor, kizárt session "(kizárva)" jelöléssel + külön megjegyzés-sorral), záró "Összegzés" szakasz (session-szám, összes usable integráció, cél haladás % ha van `goal:Xh` cím). Fájlnév-sanitizálás a meglévő `Sanitizer.sanitize` hívásával (target-részhez), időbélyeg-formátum azonos a `SuggestionScript`-ével (`yyyyMMdd-HHmmss`). CLI: `astrotool export --target T --format astrobin\|csv\|md [--out PATH] [--root R]` — alapból `.astro_tool/exports/`-be ír `WriteGuard`-on át és kiírja az útvonalat, `--out -` stdino-ra írja a tartalmat (nincs fájl), `--out PATH` a könyvtáron KÍVÜLI útvonalra sima `FileManager`-rel ír (a könyvtáron belüli `--out` elutasítva — ott csak a `WriteGuard` saját `.astro_tool/` útvonalai legálisak), hiányzó `--target`/`--format` exit 1. App: Statisztika fül célpont-sorának Műveletek cellájában "Exportálás…" menü (AstroBin CSV / CSV / Markdown) → `AppState.exportAcquisition(target:format:)` háttérművelet → Finder-reveal, ugyanaz a minta mint `generateSuggestions()`. Új tesztfájl `Tests/AstroCoreTests/AcquisitionExportTests.swift` (11: fejléc pontos egyezés, session×nominális-expozíció csoportosítás helyes szám/időtartammal, OSC szűrő üres, DSLR sensorCooling üres, calib-számok + flatDarks-számítás, `_hibas` kihagyva astrobin-ból, CSV-escaping vesszős tag-gel, CSV frames_usable/rejected, md célpont-fejléc+integráció, md `_hibas` "(kizárva)"-jelölés de benne marad, `write()` `.astro_tool/exports` alatt helyes kiterjesztéssel és `render()`-rel egyező tartalommal), `CLISmokeTests` +3 (`--out -` stdout-mód, alapértelmezett fájl-mód útvonal-ellenőrzéssel, hiányzó `--target`/`--format` hibakód). |
| R6-1 Kalibráció-egészség riport (flat-fegyelem, bias-készlet, dark-készlet) | ✅ | 484 teszt zöld (468 + 16 új); asztrofotós szakértői indíttatás: OSC deep-sky-nál a FLAT a #1 minőségi tétel (vignettálás + szenzorpor korrekció), és NEM vihető át session-ök között, ha az optikai vonat elfordult (`ROTATOR`), a gyújtótáv változott, vagy a por elmozdult (időrés) — a felhasználó `calibration_library/flats/` mappája ÜRES (minden flat session-lokális), ezért a per-session flat-fegyelem a lényeg. Új `Sources/AstroCore/Calib/CalibHealth.swift` (`FlatDiscipline`/`BiasGroup`/`DarkMasterHealth`/`CalibHealthReport`, `CalibHealth.report(db:config:)`) — három blokk: (a) **flat-fegyelem** minden usable lighttal rendelkező session-re (`FrameSet.lightBuckets` a dedupra) — `"nincs flat"` ha egyáltalán nincs flat, `"flat nem illik"` a `reasons`-ben felsorolt okokkal (gyújtótáv `fits_meta.focallen` ±2mm, szűrő `fits_meta.filter`, `ROTATOR`-szög a `header_json`-ból `calib.rotatorToleranceDeg`-en túl — csak ha MINDKÉT oldalon van érték —, flat-kor `calib.flatMaxAgeDays`-en túl DATE-OBS mindkét oldalon, medián időpontokkal), egyébként `"rendben"`; (b) **bias-készlet**: MINDEN bias frame (session + `calibration_library`) csoportosítva (gain, offset, kamera) szerint `fits_meta`-ból, csoportonként darabszám + hely (a fájl szülő könyvtára), plusz `missingBiasCombos`: a usable lightok által ténylegesen használt (gain/offset/kamera) kombók, amikhez NINCS bias-csoport (`"nincs bias: gain100/offset50/..."`); (c) **dark-készlet egészség**: minden master dark könyvtárra (`CalibAnalyzer.masterDirInfos(db:)` — új publikus API, a meglévő privát `masterDirs`/`MasterDir` bővítve `fileCount`/`ccdTemps` mezőkkel, nem duplikálva a felfedezési/DATE-OBS-kor logikát) kor (nap), elavult-jelző, CCD-TEMP medián ± max eltérés (>1.5°C → `"instabil hőmérséklet"` figyelmeztetés), keretszám, és `isUnused` (`"nem használt"`) azokra a masterekre, amiket `CalibAnalyzer.coverage()` egyik jelenlegi (exp,temp,gain,offset,kamera) kombója sem talált el — ezzel nincs duplikált illesztési logika, a coverage saját `matchedMasterPath`-jait használjuk fel forrásigazságként. `AstroConfig.calib` additív mezői: `flatMaxAgeDays: Int = 30`, `rotatorToleranceDeg: Double = 2.0`. CLI: `astrotool calib --health [--json]` — `--health` nélkül a meglévő coverage-riport változatlan; emberi kimenet három magyar fejlécű blokk (Flat-fegyelem / Bias-készlet / Dark-készlet egészség), problémák elöl. App: `CalibrationView` a meglévő lefedettségi táblázat alá "Kalibráció-egészség" szakaszt kapott három `DisclosureGroup`-pal (piros/narancs/zöld status-pöttyökkel), `AppState.calibHealth`/`loadCalibHealth()` (a `runScan()` is frissíti a Stats/Calib mellett). Új tesztfájl `Tests/AstroCoreTests/CalibHealthTests.swift` (12: hiányzó flat, gyújtótáv-eltérés, rotátor-eltérés a tűrésen túl + rotátor hiányában nincs ok, flat-kor 30 napon túl, mindenben rendben session, bias-csoportosítás, hiányzó bias-kombó észlelése, dark-master hőmérséklet-szórás figyelmeztetés, használt/nem-használt master megkülönböztetés, JSON round-trip), `CLISmokeTests` +3 (`calib --health --json` dekódolás, emberi fejlécek, `--health` nélküli mód változatlan), `ConfigTests` bővítve (`flatMaxAgeDays`/`rotatorToleranceDeg` alapérték + partial-decode). |
| R6-2 Éjszakai hardver-egészség (hűtő-stabilitás + fókusz-trend, `health` parancs) | ✅ | 499 teszt zöld (484 + 15 új); nyáron az ASI2600 hűtője nem biztos, hogy tartja a -20°C célhőmérsékletet — ha a `CCD-TEMP` eltér a `SET-TEMP`-től, a dark-kalibráció csendben lerontódik (mindkettő `fits_meta` oszlop, keretenként); a FWHM éjszakán belüli emelkedése fókuszcsúszásra/páralecsapódásra utal, a tanulság a KÖVETKEZŐ éjszaka újrafókusz-intervallumát alapozza meg (`ratings.fwhm` ⋈ `fits_meta.date_obs` trendje). Új `Sources/AstroCore/Stats/NightHealth.swift` (`CoolerHealth`/`FocusHealth`/`NightHealthReport`, `NightHealth.report(target:date:db:config:)`) — (a) **hűtés**: `FrameSet.lightBuckets` dedupolt usable lightjaira a páros (mindkét oldal nem-nil) `CCD-TEMP - SET-TEMP` eltérések mediánja, max abszolút eltérése, és a `calib.coolerToleranceC`-n (új additív mező, alapból 1.0°C) túli keretek aránya — `"stabil"` ha a küszöb feletti arány ≤10%, egyébként `"hűtő nem tartja a célhőmérsékletet (max +X.X°C, a keretek N%-án)"`, `"n/a — nincs hűtési adat"` ha egyetlen keretnek sincs páros hőmérséklet-adata (DSLR); (b) **fókusz**: a session pontozott (fwhm+DATE-OBS) keretjeinek egyszerű legkisebb-négyzetek lineáris regressziója (óra, fwhm-px) párokon — a meredekséget arcsec/órában adja ha van pixel-skála (`xpixsz`+`focallen`, a `SessionQuality`-ből kiemelt megosztott `pixelScaleArcsec(xpixsz:focallen:)` helperen át), egyébként px/órában; `"stabil fókusz"` ha `|teljes drift|` a küszöb (0.3" vagy 0.15px) alatt, `"fókuszcsúszás gyanú (+D/H óra)"` pozitív túllépésnél, `"javuló FWHM (lehűlés/seeing) (...)"` negatívnál, `"n/a — kevés pontozott keret"` 5-nél kevesebb (fwhm+DATE-OBS) keretnél. Új megosztott `CoolerStats.pairedDeltas(_:)` (`NightHealth.swift`-ben, internal) + `NightHealth.coolerOutOfBandFractionThreshold` (10%) — ugyanezt a párosítás-konvenciót és küszöböt használja az új `cooler-not-reaching-setpoint` audit szabály (`Sources/AstroCore/Audit/Rules.swift`, 16. szabály, `suspicious`, session-önként `sessions/<target>/<date>` útvonallal), regisztrálva `AuditEngine.defaultRules()`-ben — a két feature sosem térhet el abban, mi számít "nem tartja a célhőmérsékletet"-nek. `AstroConfig.calib.coolerToleranceC: Double = 1.0` additív mező. CLI: `astrotool health --target T [--date D] [--root R] [--json]` — `--date` nélkül a célpont `SessionStatsQueries.sessions` szerinti összes session-dátuma, két sor session-önként (Hűtés/Fókusz verdict). App: Minőség fül kiválasztott session-sorának idővonal-sora alá második, színkódolt sor (`AppState.nightHealth`, `loadSessionTimeline` egy háttér-hop alatt tölti be a timeline-nal együtt) — zöld "stabil"/"stabil fókusz", narancs "gyanú"/"nem tartja", szürke az "n/a" esetekre. Új tesztfájl `Tests/AstroCoreTests/NightHealthTests.swift` (11: hűtés stabil/instabil pontos aránnyal/DSLR n/a, fókusz-regresszió pontos meredekség szintetikus monoton FWHM-en px-ben és arcsec-ben, <5 pontozott keret n/a, stabil fókusz küszöb alatt, audit szabály tüzel a >10%-os fixture-ön és csendben marad a stabilon és a hőmérséklet-adat nélkülin, JSON round-trip), `CLISmokeTests` +4 (`health --json` dekódolás, emberi Hűtés/Fókusz sorok, `--target` nélkül hibakód, `--date` szűrés), `ConfigTests` bővítve (`coolerToleranceC` alapérték + partial-decode). |
| R6-3 Mozaik-panel követés + setup-fingerprint (WCS-klaszterezés, `panels` parancs) | ✅ | 529 teszt zöld (499 + 30 új); a szélesmezős mozaikoknál (pl. `M_Milky_Way/Panel1..Panel11`) a panelek közti egyenlőtlen integráció látható SNR-lépcsőt okoz a varratoknál — a plate-solve-olt WCS (`CRVAL1`/`CRVAL2`) alapján a keret-középpontok klaszterezése felfedi a paneleket és azok integráció-egyensúlyát; a másik gyakori hiba, hogy egy session usable lightjai (vagy egy célpont session-jei) eltérő eszköz-összeállítással (kamera/gyújtótáv/pixelméret) készülnek, ami csendben rontja a stack minőségét. Új `Sources/AstroCore/Sky/FieldGeometry.swift` (`FrameField`/`Panel`/`PanelReport`, `FieldGeometry.frameField(headerJSON:naxis1:naxis2:)`/`panels(target:db:config:)`) — `frameField` a kötelező `CRVAL1`/`CRVAL2`-t, a rotációt és pixel-skálát a WCS `CD` mátrixból (`CD1_1..CD2_2`, `sqrt(|det|)·3600` skála, `atan2(CD1_2,CD1_1)` rotáció — egy tetszőleges szöggel elforgatott, skálázott forgatómátrix determinánsa mindig a skála négyzete, ez teszi tesztelhetővé mindkettőt egyetlen konstruált mátrixból), vagy `CD` mátrix hiányában a meglévő `SessionQuality.pixelScaleArcsec` (`xpixsz`+`focallen`) helperrel adja vissza (utóbbi esetben rotáció nélkül); a látómező `NAXIS1`/`NAXIS2` × skála, csak ha van skála. `panels` a célpont ÖSSZES session-jének usable lightjait (`FrameSet` dedup) klaszterezi mohó egykapcsolatú (union-find alapú single-linkage) módszerrel a nagykör-távolság (`SunMoon.angularSeparationDeg`, már publikus, újrahasznosítva) alapján — összekapcsolási küszöb az ismert látómező-szélességek mediánjának fele, vagy 1.0° ha egyetlen keretnek sincs ismert látómezeje; a panel középpontja a tagok RA/Dec-jének EGYSÉGVEKTOR-átlaga (nem naiv számtani átlag — helyesen kezeli a 0°/360° RA-átfordulást, pl. 359.9°+0.1° kb. 0.0°-ra klaszterez, nem a szemközti 180°-ra), keretszám szerint csökkenő sorrendben A/B/C…-vel címkézve; `isMosaic` (`panels.count >= 2`), `isUnbalanced` (legalább két nemnulla integrációjú panel közül legnagyobb/legkisebb aránya > 1.5, pl. 2:10 vs. 0:35). Új `Sources/AstroCore/Stats/EquipmentProfile.swift` (`SetupFingerprint`, `EquipmentProfile.fingerprint(meta:headerJSON:)`/`sessionFingerprints(target:date:db:config:)`) — kompakt eszköz-ujjlenyomat leíró (kamera + gyújtótáv egész mm-re + pixelméret 2 tizedesre + binning ha van (`XBINNING`/`YBINNING` a `header_json`-ból) + Bayer-minta (`BAYERPAT`) + guide-kamera ha van (`GUIDECAM`)), pl. `"ASI2600MC·302mm·3.76µm·RGGB"`; `nil` ha a keretnek se kamerája, se gyújtótávja, se pixelmérete nincs. Két új audit szabály (`Rules.swift`, 17-18., regisztrálva `AuditEngine.defaultRules()`-ben): `mixed-setup-in-session` (suspicious — egy session usable lightjai ≥2 különböző fingerprintre esnek szét) és `mixed-setup-in-target` (probablyIntentional — egy célpont session-jeinek DOMINÁNS fingerprintje eltér session-ök között, pl. gyújtótáv-váltás — gyakran szándékos, csak figyelmeztetés), mindkettő a megosztott, DB-mentes `EquipmentProfile.fingerprintCounts`/`dominant` helpereken át (a fingerprint nélküli keretek mindkét szabálynál figyelmen kívül maradnak), hogy sose térjenek el a `sessionFingerprints`-től abban, mi számít ugyanannak a setupnak. `SessionDetail` additív `setupDescriptor: String?` mezője (a session domináns fingerprintjének leírója). CLI: `astrotool panels --target T [--json]` — emberi táblázat (PANEL / KÖZÉP RA/DEC / KERET / INTEGRÁCIÓ / ROT / SCALE) + `"⚠️  kiegyenlítetlen mozaik"` sor ha `isUnbalanced`, `--target` nélkül exit 1. App: `AppState.panelReportsByTarget` (a `loadStats()` minden célponthoz kiszámolja a `sessionDetailsByTarget` mellett egyazon háttérműveleten); `StatsView` célpont-sorának név-tooltipje mozaik célpontnál "Panelek: 3 panel: A 2:10 · B 1:50 · C 0:35 ⚠️ kiegyenlítetlen" sort kap, a Műveletek cellában az Exportálás… menü mellé "Panelek…" gomb kerül (csak mozaik célpontokon), ami popoverben teljes panel-táblázatot mutat (label/közép RA-Dec/keretszám/integráció/rotáció/pixel-skála + az egyensúly-figyelmeztetés). Új tesztfájlok: `Tests/AstroCoreTests/FieldGeometryTests.swift` (12: `CRVAL`-parszolás bare/hiányzó esetekkel, `CD`-mátrixból pontos skála+rotáció, `XPIXSZ`/`FOCALLEN` fallback, `NAXIS`-ból látómező, egyetlen mező nem mozaik, két 3°-ra lévő 1° látómezős csoport 2 panelre klaszterezése, kiegyenlítetlen/kiegyensúlyozott integráció, RA-átfordulás egységvektor-átlaga, üres célpont, JSON round-trip), `Tests/AstroCoreTests/EquipmentProfileTests.swift` (15: fingerprint-leíró pontos formátuma, binning ki-/bekapcsolva, guide-kamera, `nil` azonosító adat nélkül, kerekítés, session-önkénti darabszám, domináns kiválasztás, `SessionDetail.setupDescriptor` mindkét ág, JSON round-trip, mindkét audit szabály tüzelése/csendje + fingerprint nélküli keretek kihagyása), `CLISmokeTests` +3 (`panels --json` dekódolás, emberi "no WCS-solved frames" üzenet CRVAL nélküli fixture-ön, hiányzó `--target` hibakód). |
| R6-4 README-indexelés (kereshető éjszaka-napló, Bortle/SQM az exportba, `search` parancs) | ✅ | 551 teszt zöld (529 + 22 új); Bortle/SQM/seeing/dew/megjegyzés sosem kerül FITS fejlécbe, de a felhasználó munkafolyamata már most is beírja őket minden session `README.txt`-jének "Fill in metadata" szakaszába. Séma v5: új `session_notes(target, session_date, key, value)` tábla (`PRIMARY KEY(target, session_date, key)`), `Database.upsertSessionNotes(target:date:notes:)` (session-önkénti teljes csere — delete-then-insert a class saját lock-ján belül; ez a SAJÁT `.astro_tool` DB-je, nem a képkönyvtár, a vasszabály erre nem vonatkozik), `sessionNotes(target:date:)`, `searchNotes(query:)` (SQLite `LIKE`, ASCII-re alapból kis-nagybetű-független). Új `Sources/AstroCore/Scan/ReadmeNotesParser.swift` (`parse(text:)`/`parse(data:)`) — `^([A-Za-z][A-Za-z0-9 ()/_-]{0,40}):\s*(.*)$` mintára illeszkedő sorok kulcs/érték párokra bontása (a template saját fejléc-kulcsai — `Target folder`, `Date`, ... — is tárolódnak, harmlessek és hasznosak), üres érték kihagyva, 64 KiB felett vagy nem-UTF8 tartalomnál `nil`. Scan-integráció a meglévő FITS-meta-capture ág mellett (`Scanner.captureMeta`/`captureReadmeNotes`): minden ÚJ/MEGVÁLTOZOTT session-szintű `README.txt` (`sessions/<target>/<date>/README.txt` pontosan — egy role-alkönyvtár alatti névazonos fájl NEM az, a `PathClassifier` `.other` szerepe dönti el) újraparszolódik; `--refresh-meta` egy változatlan README-t is újraolvas, ha még nincs hozzá `session_notes` sora. CSAK OLVAS — a `README.txt`-t a scanner soha nem írja. `SessionDetail` additív `notes: [String: String]` mezője (`SessionStatsQueries` tölti); az app Statisztika fülének "README" jelvényén `.help` tooltip listázza a `kulcs: érték` sorokat. CLI: `astrotool search <query> [--root R] [--json]` — cél/dátum szerint csoportosított emberi kimenet, pozicionális `<query>` argumentum (`tag add`/`tag remove` mintájára `splitPositionalArgs`-szal), nincs találatnál is exit 0. Export: `astrobin` formátum eddig üresen hagyott `bortle`/`meanSqm` oszlopai a session jegyzeteiből töltődnek — `bortle` az első "Bortle"-t tartalmazó kulcs értékéből az első ÖNÁLLÓ 1-9 számjegy (pl. `"4"` vagy `"falu, 4"` is `"4"`, de `"42"` végződése NEM önálló), `meanSqm` az első "SQM"-et tartalmazó kulcs értékéből az első 16-22 tartományba eső decimális szám (a tartományon kívüli számok — pl. egy műszer-sorozatszám — átugorva, nem megállítva a keresést); megfelelő kulcs/szám nélkül üresen marad, nem tippel. Tervező/minőség érintetlen. Új tesztfájl `Tests/AstroCoreTests/ReadmeNotesParserTests.swift` (7: valódi `SessionCreator`-sablon fejléc-kulcsai megvannak + üres mezők kihagyva, egyéni kulcs (SQM) elfogva, colon/kezdő-betű nélküli sor kihagyva, 64 KiB felett/nem-UTF8-nál `nil`, üres szövegre `[:]`), `DatabaseTests` +5 (v4→v5 migráció táblával+meglévő sor érintetlen, replace-all szemantika, cél/dátum szerinti izoláció, üres alapérték, `searchNotes` kulcs/érték LIKE kis-nagybetű-független), `ScannerTests` +5 (új README elfogva, role-alkönyvtárbeli névazonos fájl NEM az, megváltozott README újraolvasva replace-all-lal, változatlan README NEM íródik újra, `--refresh-meta` pótolja a hiányzó jegyzeteket), `SessionStatsTests` +1 (`SessionDetail.notes` README-vel/nélküle), `AcquisitionExportTests` +2 (bortle és meanSqm: sima számjegy/beágyazott szám/hiányzó → üres), `CLISmokeTests` +2 (`search` találat a fixture README-jén, találat nélkül is exit 0). |
| R7-1 Plate-solve backfill (Siril blind-solve, `solve` parancs) | ✅ | 579 teszt zöld (551 + 28 új); az ASIAIR FITS lightok mindig plate-solve-oltak (`CRVAL1`/`CRVAL2` a fejlécben), de a wide-field Canon CR3 célpontoknak nincs FITS fejlécük egyáltalán → a `Planner`/`FieldGeometry` "nincs koordináta"-t ad, sem terv, sem panel-bontás. Séma v6 (additív `ALTER TABLE`): `fits_meta.solved_ra`/`solved_dec`/`solved_scale_arcsec`/`solved_rotation_deg` — SOSEM a `header_json`-on (az eredeti szkennelt fejléc) keresztül írva, hanem egy dedikált `Database.updateSolvedWCS(fileID:ra:dec:scale:rotation:)` célzott `UPDATE`-jén át, hogy egy KÉSŐBBI rescan (`upsertFITSMeta`, aminek `ON CONFLICT` klózja minden oszlopot a hívó rekordjából ír felül) sose törölhesse vissza némán egy már megoldott koordinátát (a scanner épített `FITSMetaRecord`-ja sosem hordoz solved-mezőt) — regresszió-teszttel lefedve. Új `Sources/AstroCore/Sky/PlateSolver.swift`: `SolveBackend` protokoll (`solve(originalPath:workDir:) throws -> URL`) — a produkciós `SirilSolveBackend` a `SirilCLI` mintáját követi (`siril-cli -s -` stdinen kapott script, háttérszálas blokkoló stdout/stderr-olvasás semaphore-ral), a script `cd "<workDir>"` + `load "<eredeti fájl, csak olvasva>"` + `platesolve` + `save solved` + `close` — a `save` a Siril FOLYAMAT aktuális munkakönyvtárába ír (ezért a `cd` a scratch mappába, SOSEM az eredeti fájl könyvtárába), a megoldott `solved.fit`/`.fits`-et a saját `FITSReader`-ünk olvassa vissza a `CRVAL`/`CD` kártyákért — a könyvtár egyetlen fájlja sem módosul, csak a DB. Az útvonal-injekció elleni védelem (`"`/`\` karakterek) kiemelve megosztott `containsSirilScriptInjectionRisk`-be (`SirilCLI.swift`), amit mind a `SirilCLI.buildScript`, mind `SirilSolveBackend.buildScript` hív — sosem térhetnek el. `PlateSolver.solveTarget(_:db:config:maxFramesPerSession:force:progress:)` célpontonként session-önként választ ki legfeljebb `maxFramesPerSession` (alap 1) usable (`FrameSet.lightBuckets`), koordináta nélküli (`TargetCoordinates.coordinates` fejléc+solved-oszlop ellenőrzéssel, `force` felülírja) keretet, a path szerint rendezett lista KÖZEPÉről (`index count/2`) — egy session-en belül ez egy jó reprezentatív pointing, nem kell minden keretet megoldani. Minden keret saját, `FileManager.temporaryDirectory` alatti scratch-alkönyvtárat kap (a batch-workdir a `Rater` őrzött-törlés mintáját követi), egyedi keret-hiba (load/solve/timeout/parse) `SolveResult.error`-ban rögzítve, a batch folytatódik. `FieldGeometry.scaleAndRotation(cd11:cd12:cd21:cd22:)` — kiemelt, megosztott CD-mátrix→skála/rotáció helper (`frameField` saját CD-ága ÉS `PlateSolver` egyaránt ezt hívja, sosem térhetnek el). Tesztelhetőség: `PlateSolver` egy injektálható `SolveBackend`-en keresztül dolgozik (a valós Siril-backend csak az alapértelmezett), a tesztek egy mock backendet adnak át, ami szintetikus megoldott FITS-et ír a workDir-be. `TargetCoordinates.coordinates(headerJSON:solvedRA:solvedDec:)` és `FieldGeometry.frameField(headerJSON:naxis1:naxis2:solvedRA:solvedDec:solvedScaleArcsec:solvedRotationDeg:)` additív paraméterekkel bővítve — a fejléc WCS-e elsőbbséget élvez, a solved-oszlopok csak akkor esnek latba, ha a fejléc (vagy annak hiánya — egy CR3-nak nincs is FITS fejléce) nem ad koordinátát; `medianCoordinates`/`FieldGeometry.panels` hívási helyei frissítve, hogy a `fits_meta` solved-oszlopait is átadják. CLI: `astrotool solve --target T\|--all [--frames N] [--force] [--root R] [--json]` — `--all` minden koordináta nélküli célpontot bejár (`TargetCoordinates.medianCoordinates` alapján), ismeretlen `--target` exit 1 (MÉG a Siril-ellenőrzés előtt fut, hogy a hibaüzenet akkor is helyes legyen, ha a gépen tényleg van telepített Siril), hiányzó Siril exit 1 világos üzenettel, egyébként MINDIG exit 0 (részleges hibák/0 megoldott keret esetén is) — a per-célpont `SolveSummary` (attempted/solved/failed/skipped) emberi táblázat vagy `--json`. App: Statisztika fül célpont-sorának Műveletek menüje "Plate-solve…" gombot kap, DE csak akkor, ha `AppState.plan` (már betöltve a fülre lépéskor) szerint a célpontnak nincs koordinátája — `PlateSolveSheet` (a `CalibLinkSheet` mintájára) futtatja a háttérműveletet progress-szel, majd a végeredményt mutatja, és a `loadStats()`/`loadPlan()` újrafut, hogy az újonnan megoldott koordináta azonnal megjelenjen. Új tesztfájl `Tests/AstroCoreTests/PlateSolverTests.swift` (14: keretkiválasztás a path-rendezett lista közepéről, `force` felülír egy már meglévő koordinátát, session-önkénti középső-koordinátanélküli keret kiválasztása, mock solve → `solved_*` perzisztálva pontos skálával/rotációval egy ismert CD-mátrixból, keret-hiba rögzítve és a batch folytatódik, üres célpont nullázott summary, workDir mindig a temp könyvtár alatt sosem a fixture-könyvtárban, progress callback minden megkísérelt keretre, `PlateSolver.init` `sirilNotFound`, `SirilSolveBackend.buildScript` tartalom + injekció-elutasítás), `DatabaseTests` +3 (v5→v6 migráció táblaérintetlenséggel + `updateSolvedWCS` azonnal használható, `updateSolvedWCS` nem nyúl a `header_json`-hoz/más oszlopokhoz, rescan-regresszió: egy `upsertFITSMeta` sosem törli vissza egy korábban megoldott koordinátát), `TargetCoordinatesTests` +4 (solved-oszlop fallback nil fejléc esetén, fejléc CRVAL elsőbbsége solved felett, egyik oldal sem old fel + `medianCoordinates` solved-oszlopokból), `FieldGeometryTests` +4 (`frameField` solved-fallback fejléc nélkül, fejléc elsőbbsége, egyik sem old fel, `panels` kizárólag solved-oszlopokból épített mezőkkel), `CLISmokeTests` +3 (`--target`/`--all` nélkül hibakód, ismeretlen `--target` hibakód a "target not found" üzenettel, Siril hiánya config.json `rating.sirilPath` felülbírálással determinisztikusan kikényszerítve — függetlenül attól, van-e valódi Siril a gépen). |
| R7-B1 64×-es bias-bug javítás + per-Bayer háttér + `sensor` parancs + Siril-adapter javítás | ✅ | 625 teszt zöld (579 + 46 új); szakértői review (`docs/superpowers/reviews/2026-08-03-astrophotographer-review-2.md`) által valós DB-n talált 4 tétel. **(A) ⛔ KRITIKUS bug**: `SessionQuality.backgroundEPerSecPerArcsec2` sosem vonta le a bias-pedesztált (~501 ADU az ASI2600-on gain100/offset50) — valós Rosette-adaton 0.147 e⁻/s/arcsec² jelentett a mért 0.0023 igazsághoz képest, ~64×-es felfújás minden `quality`/`health`/export e⁻-mértékű számban a 0.4.0 óta. Javítás: `max(0, (background_ADU − biasLevel) × EGAIN / EXPTIME / scale²)`; `biasLevel` az új `sensor_profile` táblából EXAKT `(camera, gain, offset)` egyezéssel (sosem gain-only/kamera-only fallback — dokumentálva), nincs mért profil a kombóhoz → `nil`, sosem hibás szám. **(B)** `NativeStats` ugyanabban a pixel-passzban négy Bayer-parity mediánt is számol (`backgroundMedian00/01/10/11`, `(row%2,col%2)` szerint) az összesített medián mellett; új `BayerMap.channelMedians(stats:bayerPattern:)` RGGB/BGGR/GRBG/GBRG mintát R/G/G/B-re map-el (a két zöld pozíció átlagolva, ismeretlen minta → mindhárom `nil`); séma v7 additív `ratings.bg_00/01/10/11`, `Rater` perzisztálja rate közben. **(C)** Új `Sources/AstroCore/Stats/SensorProfile.swift` (`SensorProfileRecord`, `SensorProfiler.measure`/`combosMissingProfile`) — kamera/gain/offset kombónként: bias-szint (egy bias frame központi 50%-os cropjának mediánja, `NativeStats.centralCropPixels` új helper), leolvasási zaj (KÉT bias frame különbségének 5σ-klippelt szórása / √2 × EGAIN — szándékosan NEM MAD, mert az ADU-kvantálás (~4 e⁻/ADU) alulméri: a szakértő 1.02 e⁻-t mért MAD-dal a helyes 1.30 e⁻ helyett klippelt szórással), dark-ráta (`(dark_median − bias_level) × EGAIN / EXPTIME`, 0-ra clampelve — a 0 is valós eredmény, a kvantálási küszöb alatt), EGAIN (`fits_meta.egain` mediánja); séma v7 `sensor_profile(camera,gain,offset)` tábla + `Database.upsertSensorProfile`/`sensorProfile`/`allSensorProfiles`. CLI `astrotool sensor [--measure] [--json]` — `--measure` nélkül csak a már tárolt profilokat listázza (nem mér semmit), `--measure`-rel újramér a tracked BIAS/DARK keretekből és perzisztál; mindkét módban stderr drift-figyelmeztetés minden olyan `(camera,gain,offset)` kombóra, amit usable light frame ténylegesen használ, de nincs hozzá használható mért profil ("nincs mérés ehhez: KAMERA · gain G · offset O (készíts legalább 2 bias frame-et)"). App: Kalibráció fül új "Szenzor-profilok" read-only táblázata + "Mérés" gomb (`AppState.loadSensorProfiles()`/`measureSensorProfiles()`, `SensorProfileSection` in `CalibrationView.swift`). **(D)** `SirilCLI` a valós gépen (Siril 1.4.4, macOS app-bundle) csendben nem működött: (1) a verzió-parszolás korábban az ELSŐ sort tartotta meg vakon, ami a "Siril is started as macOS application" indítási banner (nincs benne számjegy) — az éles DB mind az 586 sorában ez landolt `siril_version`-ként a valós "siril 1.4.4" helyett; javítva `parseVersionOutput`-tal (első "siril"+számjegyet tartalmazó sor → utolsó verziószám-mintás sor → "unknown"). (2) a `findstar`-kimenet regex-e (`Found\s+(\d+)\s+star`) nem illeszkedett a valós Siril 1.4 szövegére ("Found 5 Gaussian profile stars in image, channel #0 (FWHM ...)" — extra szavak a szám és a "star" közt), ezért az éles adatbázisban `fwhm`/`roundness`/`star_count` 100%-ban NULL volt; javítva nem-mohó `.*?`-vel a szám és "stars?" közt (egy soron belül marad, nem szökik át a következő sorba). Mindkét javítás valódi `siril-cli` bináris ellen (nem csak mock) integrációs teszttel ellenőrizve, egy szintetikus Gauss-csillagmezőn temp könyvtárban (a valós képkönyvtár sosem érintve) — star_count ≥ 1, fwhm > 0 ténylegesen visszajön. (3) új `Rater.shouldWarnNoMetrics` szerkezeti őr: ha egy Siril-providerrel futtatott ≥5 keretes batch egyetlen keretre sem ad metrikát, `astrotool rate` stderr figyelmeztetést ír ("a Siril nem adott metrikát egyetlen keretre sem — ellenőrizd a telepítést") — pont az a csendes hiba-mód, ami az éles adatbázison észrevétlen maradt 586 soron át. Új tesztfájl `Tests/AstroCoreTests/SensorProfileTests.swift` (14), `RateTests`/`DatabaseTests`/`SessionQualityTests`/`CLISmokeTests` bővítve (per-Bayer medián pontosság + degenerált-frame `nil`, `BayerMap` mind a 4 mintára, `centralCropPixels` pontos crop + `.fz` guard, séma v6→v7 migráció, `sensor_profile`/`ratings.bg_*` round-trip, bias-pedesztál levonás pontos számmal + hiányzó profil `nil` + exakt-egyezés nincs fallback + negatív clamp, valós Siril-verzió/findstar regressziós tesztek, `sensor` CLI --measure/--json/emberi/drift-figyelmeztetés). |
| R7-B2 DSS ingest (`.info.txt` metrikák + `.dssfilelist` döntések) | ✅ | 650 teszt zöld (625 + 25 új — 14 `DSSIngestTests` + 8 `DatabaseTests` + 2 `SessionStatsTests` + 1 `RateTests`; emellett a 7 meglévő séma-migrációs teszt "latest version" assertje 7-ről 8-ra frissült, mivel `Database(path:)` mindig a legfrissebb sémáig migrál). A könyvtárban 346 kihasználatlan DeepSkyStacker `<frame>.info.txt` (mért csillag-metrika sidecar minden lighthoz) és `.dssfilelist` (a felhasználó saját CHECKED accept/reject döntése) hevert — ez a review a `Rater`/`SessionQuality` mellé egy második, ingyenes metrika-forrást ad. Séma v8 (additív): `ratings.source TEXT` (`NULL` = astrotool/Siril, `"dss"` = `DSSIngest` írta) + új `user_verdicts(file_id PK, accepted, source, recorded_at)` tábla. Új `Sources/AstroCore/Scan/DSSIngest.swift`: `DSSInfoParser.parse(data:)` defenzív `Key = value` parser (256 KiB olvasási sapka, `Quality` mint `OverallQuality` alias, ismeretlen sor csendben eldobva) — `fwhm ≈ 2×MeanRadius` (sugár→átmérő közelítés, dokumentálva), `roundness ≈ Circularity`, hiányában a per-csillag `Axises = a, b` sorok `min/max` arányának átlaga esik vissza rá, `star_count = NrStars`. `DSSFilelistParser.parseRows`/`resolvedLightRows` — csak a `light`-típusú sorok számítanak (dark/flat/bias/offset eldobva), a FILE mezőt a lista SAJÁT könyvtárához oldja fel (kézi `.`/`..` komponens-normalizálás, `\` → `/` a Windows-natív DSS-kimenet miatt). `DSSIngest.ingest(db:config:root:)` a DB-t járja be (nem a lemezt): minden `path.hasSuffix(".info.txt")` tracked sorhoz megkeresi az azonos könyvtárbeli, kiterjesztés nélküli LIGHT testvért, és csak akkor ír `ratings` sort `source = "dss"`-sel, ha ahhoz még nincs `source IS NULL` (valódi astrotool/Siril) rating — ez sosem íródik felül; egy már beolvasott, változatlan `input_sig`-ű `dss`-sor újrafuttatáskor kihagyva (idempotens). Minden tracked `.dssfilelist`-ből a `light` sorok `user_verdicts`-be kerülnek. `Rater` semmilyen külön esetet nem igényelt: egy `dss`-sorsú rating, ha az `input_sig`-je egyezik, pontosan úgy cache-hit, mint bármely más — a provider sosem hívódik rá, mégis részt vesz a z-score számolásban (tesztelve). `SessionStats.SessionDetail` additív `dssAcceptedCount`/`dssRejectedCount` (`Database.acceptedCounts(target:date:)`-ből, `nil` együtt, ha nincs rögzített döntés — sosem hamis "0✓/0✗"). CLI `astrotool ingest-dss [--root R] [--json]` — szándékosan NEM fut automatikusan a `scan --refresh-meta`-val (dokumentálva a `--help`-ben: egy DSS-fa nagy tud lenni, ez marad explicit lépés). App: Statisztika fül session-sorának "Keretek" oszlopa " · DSS: N✓/M✗" jelvényt kap, ha van rögzített döntés; Áttekintés fül "DSS-adatok beolvasása" gomb (csak akkor jelenik meg, ha `Database.hasTrackedFileWithSuffix(".dssfilelist")` igazat ad — olcsó, célzott lekérdezés, sosem egy teljes `allFiles` betöltés csak erre a igen/nem kérdésre), háttérművelet + eredmény-alert. Új tesztfájl `Tests/AstroCoreTests/DSSIngestTests.swift` (14: info.txt parser teljes/részleges/`Quality`-alias/szemét/256 KiB-sapka, fwhm=2×MeanRadius, Axises-arány roundness-fallback + Circularity elsőbbsége, dssfilelist parser CRLF+nem-light sorok, relatív útvonal-feloldás `..`-vel, ingest end-to-end (rating + 2 verdict), no-clobber egy meglévő `source IS NULL` ratingen, idempotens újrafuttatás), `DatabaseTests`/`SessionStatsTests`/`RateTests` bővítve (séma v7→v8 migráció táblaérintetlenséggel, `ratings.source`/`user_verdicts` round-trip, `acceptedCounts` cél+dátum-szűrt tallyja, `hasTrackedFileWithSuffix`, `SessionDetail` DSS-számlálók valós scan+verdict-tel, `Rater` cache-hit egy `dss`-sorsú ratingen). |
| R7-B3 Expozíció-tanácsadó (sub-optimalizáló + relatív SNR, `expose` parancs) | ✅ | 666 teszt zöld (663 + 3 CLI; a 663 már tartalmazza a 13 új `ExposureAdvisorTests`-et a 650-hez képest). Mennyi legyen egy sub hossza — a mért szenzor-adatokból (`sensor_profile` bias-szintje/leolvasási zaja/EGAIN-je, R7-B1) és a mért per-Bayer háttérből (`ratings.bg_00/01/10/11`, szintén R7-B1) számolva, sosem találgatva. Új `Sources/AstroCore/Stats/ExposureAdvisor.swift`: célpontonként (vagy a `--target`-tel megadott célpont legutóbbi ÉRTÉKELT session-jére) meghatározza a domináns setup-fingerprintet (`EquipmentProfile.fingerprintCounts`/`dominant`, R6-3-ból újrahasznosítva), majd a `BayerMap.channelMedians`-szal (R7-B1) csatornánkénti mért égháttér-rátát számol (`B_ch = max(0, (median_ch_ADU − bias_level_adu) × egain / exptime)`) minden R/G/B csatornára, amihez van adat — a LEGGYENGÉBB (legalacsonyabb rátájú) csatorna szabja meg az ideális sub-hosszt, mert az a leolvasási-zaj-limitált: `t = R² / (B × ((1+C)² − 1))`, `C = config.expose.noiseContributionC` (alapból 0.05 ≡ Glover "égháttér ≥10×R²" ökölszabálya, mivel 1/((1.05)²−1) ≈ 9.76); a `C=0.10` "rövidebb subok" variáns (`≈4.76·R²/B`) mindig kiszámolva mellette, a konfigurált `C`-től függetlenül. Két sapka: `expose.maxSubSeconds` (új config mező, alapból 300s — guiding/műhold-kockázat nő a hosszal, függetlenül a tiszta zaj-matektől) és egy szaturáció-sapka (ha a session medián szaturált pixelaránya a JELENLEGI sub-hosszon már >0.1%, sosem javasol hosszabbat a jelenleginél); melyik sapka kötött, azt `capReason` ("maxSubSeconds"/"szaturáció"/`nil`) mondja meg. Leolvasási-zaj-részesedés a jelenlegi ÉS az ajánlott sub-hosszra: `(1 − √(B·t/(R²+B·t))) × 100` — mennyivel növeli a leolvasási zaj a keret zaját a tiszta foton-zajhoz képest (NGC7000-szerű R=1.3/B=0.081/t=120 adaton ≈7.7%, a szakértői review kerekített 8%-ának megfelelően). Relatív SNR-szakasz semmilyen égháttér-adatot nem igényel: a célpont eddigi, a DOMINÁNS setup-fingerprintre szűkített használható integrációjából (`totalUsableSeconds`) `snrPlus10PercentHours = 0.21 × T(óra)` (a következő +10% SNR-hez szükséges plusz óraszám, mivel (1.10)²−1=0.21) és `snrPlus3hMultiplier = √((T+3h)/T)`. Őszinte `n/a` (`notAvailableReason`), sosem hibás szám: nincs mért szenzor-profil a kombóhoz → "nincs szenzor-profil — futtasd: astrotool sensor --measure"; a keretek a per-Bayer háttér bevezetése előtt lettek pontozva (vagy még sosem lettek pontozva) → "futtasd újra: astrotool rate"; a kamerának nincs `BAYERPAT` fejléce (mono/DSLR, pl. Canon) → a funkció csak színes (Bayer) ASI-szenzorokhoz készült, a kameranév megnevezésével — ezek a checkek ABBAN a sorrendben futnak (setup-fingerprint → BAYERPAT → szenzor-profil → per-Bayer adat), hogy a hardver-szintű "ez a funkció nem alkalmazható" mindig megelőzze a "nincs még mérve" jellegű üzeneteket; a relatív-SNR mezők (`totalUsableSeconds` és utána) `notAvailableReason` esetén is kitöltve maradnak, mivel ahhoz nem kell égháttér-adat. Új `ExposeRule` config (`maxSubSeconds: Double = 300`, `noiseContributionC: Double = 0.05`), a többi Rule-lal azonos partial-decode mintával. CLI `astrotool expose [--target T] [--json] [--root R]` — `--target` nélkül egy sor/célpont táblázat (CÉLPONT/MOST/AJÁNLOTT/LEOLV.ZAJ/TANÁCS), `--target`-tel a teljes tanács minden mondata kiírva; `adviseAll` minden usable-lighttal rendelkező célpontot tartalmaz (egy hiányzó profil/Bayer-adat miatt `n/a` célpontot is — csak a 0 usable-lightú célpontok maradnak ki). App: Minőség fül, a session-összegzés fölé "Expozíció-tanácsadó" doboz a kiválasztott célpontra (`AppState.loadExposureAdvice(target:)` a meglévő `loadQualitySummaries` melletti mintával, `QualityView.exposureAdviceSection`). Új tesztfájl `Tests/AstroCoreTests/ExposureAdvisorTests.swift` (13: pontos képlet-ellenőrzés a NGC7000-szerű R=1.3/B=0.081 adaton mindkét `C`-re, leggyengébb csatorna kiválasztás, leolvasási-zaj-részesedés pontos számmal, `maxSubSeconds`-sapka `capReason`-nal, szaturáció-sapka elsőbbsége az "already fine" ellenőrzés előtt — enélkül a szaturáció-üzenet sosem jelenne meg, mivel a sapka `recommended == currentSub`-ot állít be, ami az "already fine" próbát trivialitássá tenné —, "már megfelelő" ág, mindhárom `n/a` út (nincs profil/nincs per-Bayer adat/nincs BAYERPAT), relatív SNR `+10%`/`+3h` képlet 10 órás integráción, `adviseAll` kihagyja a 0 usable-lightú célpontot), `CLISmokeTests` +3 (`expose --json` numerikus tanács `sensor --measure`+`rate --no-siril` után egy szintetikus Bayer-mintás light frame-en, hiányzó profil őszinte `n/a` JSON-ban és emberi kimenetben egyaránt, `--target` nélküli táblázat). |

## Zárás (2026-08-02)

Mind a 20 task kész. Kiadva: **v0.1.0** és **v0.1.1**
(https://github.com/themokx1/Astro-Tool/releases/tag/v0.1.1 — AstroTool.dmg +
astrotool.zip, CI zöld, 216 teszt). Négy review-kör futott (M1, T6–T9, T10–T15,
végső) — minden találat javítva és re-approve-olva.

**Nyitott tételek (user-döntés / engedély kell):**
1. GitHub Pages: a repo privát, a plan nem támogatja → tedd publikussá a repót
   (vagy fizetős plan), utána:
   `gh api -X POST repos/themokx1/Astro-Tool/pages -f 'source[branch]=main' -f 'source[path]=/docs'`
2. /Volumes/images TCC-engedély a Claude-nak → utána add_new_session.sh és
   tools/rate/ verifikáció a spec 2. szakasza szerint, plusz első valós
   (read-only) futtatás egy szűk almappán.
3. Az AstroTool appnak és a terminálnak is Teljes lemezhozzáférés kell majd az
   első éles futáshoz.
