# Task 7.1 — return-path stabilization report

## Status

Task 7.1 code closure is complete as of this update (2026-08-24, continuation session). Every item of the binding `task-7-stabilization-brief.md`, the previously outstanding normal-writer evidence-injection edge, and all Critical/Important findings of the fresh read-only Task 7 security and product-logic reviews are fixed and verified. This is still not a V5 release claim: the official iOS 26.5 build/runtime gate and the Task 8 documentation/smoke/device gate remain external and unclaimed.

## Work completed in this continuation (commits after `641f41c`)

- `6408525` — normal writers reject injected mobile evidence. `NightBriefingRevisionStore.create`/`saveIfLatest` (public) refuse caller-supplied `mobileChangeIDs`/`mobileChangeMarkers` on creation (typed `mobileEvidenceNotWritable`) and force the durable latest revision's evidence forward on every CAS update; the package-internal bridge writes markers exclusively through the new internal `saveIfLatestRecordingMobileEvidence`. `MetadataStore.createProjectAnnotation` and the creation branch of `saveProjectAnnotation` reject evidence the same way; `applyMobileProjectAnnotationBatch` remains the only annotation marker writer. Regression tests cover creation rejection, CAS preservation against both fabricated-extra and stripped evidence, and the bridge's verbatim path.
- `36e9783` — mobile return EN/HU localization completed ("Discard saved return code" in both mobile tables; the six-total outcome sentence and "Reviewed changes saved" in the Mac Hungarian table).
- `3be1754` — review fixes: the six user-facing outcome totals are now disjoint (`keptOnMac` excludes superseded IDs that share `receipt.resolvedChangeIDs` for acknowledgement purposes; the duplicated inline arithmetic in `MobileSyncView` now reads one store-computed totals value), and the three phone-side outcome messages assigned to `message` are localized in EN and HU. The localization audit script now extracts `message = "…"` literal assignments under `Sources/AstroToolMobile/`, so dynamic strings of this shape can no longer escape the EN/HU parity tests (verified: the audit failed on exactly these three keys before the strings were added).
- `410a6d3` (+ doc-comment precision follow-up) — security-review fixes: a failed `importer.apply` now releases the atomically claimed sent base back to `.published` (new exact-match `releaseClaim`, flock-serialized; safe under replay because global change-ID markers keep every retry idempotent; `discard` deliberately never touches claims so it cannot break an in-flight apply's mutual exclusion), and the resulting briefing/project-annotation notes text is capped at 1 MiB cumulative in both mobile domain bridges (typed `limitsExceeded`, fail closed, nothing written).

- `b754f86` — verification-review fix: a failed apply's retained receipt is now cleared the moment a new import preview operation begins, so `receiptTotals` can never pair a stale receipt with a fresh preview (the retry-path false-banner defect found by the adversarial verification review). Regression tests: retry-after-failure exposes no stale receipt in `.importPreviewReady`, and a coordinator-level test pins that a post-apply finalization failure (`partialReceipt` after a successful domain write) keeps the base `.claimed` (not released, not consumed) while a same-package retry completes idempotently.

## Reviews

- Read-only security review (fresh, at `36e9783`): 0 Critical, 2 Important (both fixed in `410a6d3`, above). Explicitly verified: no library-file mutation anywhere in the mobile path; single public coordinator entry point with package-only importer/sent-base/batch surfaces and non-forgeable review/token types; atomic single-use claim mechanics; marker fingerprints contain only immutable phone intent; no `Dictionary(uniqueKeysWithValues:)` traps on attacker-influenced data (uniqueness guarded and failing closed); bridge retry path cannot acknowledge undurable changes; the root flock covers marker scan + CAS write; phone-side one-time key is durably committed before export and never silently lost; no path traversal or format-string exposure from phone-controlled values.
- Read-only product-logic review (fresh, at `36e9783`): 1 Critical (totals double-count, fixed in `3be1754`), 1 Important (three unlocalized phone messages, fixed in `3be1754`). Explicitly verified: six-way classification disjointness at the importer; effective-latest semantics (malformed latest rejects the target, older records listed superseded, never silently applied); empty note never deletes and keep-both default with timestamped field note; CAS-gated checklist application with default resolutions seeded in state; partial-receipt honesty and terminal UI state; forward acknowledgement reads the reloaded ledger without relaunch; exact-ID phone queue acknowledgement with export never clearing the queue; exporter generation/cancellation guards; ordinary Mac editor flows unaffected by the writer hardening.
- Reviewer note (informational): the `appliedChangeReceipt` banner inside `incomingContent` appears unreachable with current phase transitions; it was fixed to the shared totals anyway and can be revisited as cleanup.
- Adversarial verification review of the fix commits (`3be1754`, `410a6d3`, `836cbe5`): Fix 2 and Fix 3 judged sound and complete; Fix 1 sound for its goal but one new Important defect found in the introduced `receiptTotals` (stale receipt paired with a fresh preview after retry) — fixed and regression-tested in `b754f86`. With that closure, no open Critical/Important findings remain against Task 7 code.

## Verification evidence (this session, in the V5 worktree)

- Focused suites after each fix, `--no-parallel`: NightBriefingRevisionStoreTests 9/9; MetadataStoreTests 41/41; MobileChangeImporterTests 28/28; NightBriefingStoreTests 9/9; MobileSync filter 32/32; MobileSyncStoreTests 28/28; MobileSyncSurfaceTests 6/6; MobileReturnApplicationCoordinatorTests 7/7; MobileSentSnapshot 5/5; LocalizationCoverageTests 16/16.
- Full `swift test --no-parallel`: 3509 tests, 220 suites, 0 failures (at `36e9783`; re-run at the final tree recorded below).
- Parallel stability: ProjectsStoreTests 10/10 consecutive isolated parallel runs; full default-parallel suite 3× consecutive, 3509/3509 each. No retry hid any failure.
- `xcodegen generate`: regenerated project byte-identical to the checked-in project (empty `git status`).
- Real `AstroTool` macOS scheme build (`xcodebuild -scheme AstroTool -destination platform=macOS`) in the worktree: exit 0 (only pre-existing warnings unrelated to V5). An earlier build accidentally ran in the main checkout and was discarded as evidence; the worktree build was repeated explicitly.
- `git diff --check`: clean in the worktree.
- Final full `swift test --no-parallel` at the final tree (after `b754f86`): 3524 tests, 221 suites, 0 failures; `git diff --check` clean. macOS `AstroTool` scheme rebuild repeated at the final tree: exit 0, zero errors.

## Outstanding (external gates, unchanged)

- Compile/run the checked-in `AstroToolMobileTests`/UI tests through the Xcode iOS graph when an iOS 26.5 platform/runtime is available on this machine. The official iOS runtime gate remains external and unclaimed.
- Task 8: documentation, public-content checks, deterministic smoke script, full Wave 1 gate, and the manual own-device round-trip acceptance. V5 may be called releasable only after all of these pass.
